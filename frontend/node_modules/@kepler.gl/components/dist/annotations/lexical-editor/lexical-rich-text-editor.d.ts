import { FC, ReactNode } from 'react';
import { SerializedEditorState } from 'lexical';
export type LexicalRichTextEditorProps = {
    width?: number;
    height?: number;
    editorState?: SerializedEditorState;
    initialText?: string;
    isEditable: boolean;
    children?: ReactNode;
    onChange: (config: {
        text: string;
        editorState: SerializedEditorState;
    }) => void;
};
export declare const LexicalRichTextEditor: FC<LexicalRichTextEditorProps>;
