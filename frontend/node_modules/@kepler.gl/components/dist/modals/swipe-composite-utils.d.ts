export type SwipeEasing = 'linear' | 'ease-in-out';
/**
 * Compute the swipe percentage at a given time.
 */
export declare function getSwipePercentageAtTime(timeMs: number, durationMs: number, startPct: number, endPct: number, easing?: SwipeEasing): number;
/**
 * Composite two canvases (left/right map views) with a swipe divider
 * into a single output canvas.
 */
export declare function compositeSwipeFrame(leftCanvas: HTMLCanvasElement, rightCanvas: HTMLCanvasElement, outputCanvas: HTMLCanvasElement, percentage: number, showDivider?: boolean, backgroundColor?: string): void;
