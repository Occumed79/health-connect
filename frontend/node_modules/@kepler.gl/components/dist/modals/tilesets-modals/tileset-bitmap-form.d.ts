import React from 'react';
import { BitmapBounds } from '@kepler.gl/constants';
import { MetaResponse, DatasetCreationAttributes } from './common';
type BitmapFormProps = {
    setResponse: (response: MetaResponse) => void;
};
export declare function getDatasetAttributesFromBitmap({ name, imageUrl, bounds, isDataUri }: {
    name: string;
    imageUrl: string;
    bounds: BitmapBounds;
    isDataUri?: boolean;
}): DatasetCreationAttributes;
declare const TilesetBitmapForm: React.FC<BitmapFormProps>;
export default TilesetBitmapForm;
