import React from 'react';
type GeocoderConfigProps = {
    config: {
        limitSearch: boolean;
    };
    onChange: (config: {
        limitSearch: boolean;
    }) => void;
};
declare function GeocoderConfigFactory(): ({ config, onChange }: GeocoderConfigProps) => React.JSX.Element;
export default GeocoderConfigFactory;
