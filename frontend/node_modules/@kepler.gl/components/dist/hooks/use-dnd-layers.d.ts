import { DragEndEvent, DragStartEvent } from '@dnd-kit/core';
import { Layer } from '@kepler.gl/layers';
import { LayerOrder, LayerOrderHierarchyEntry } from '@kepler.gl/types';
type DndLayersHook = {
    activeElement: LayerOrderHierarchyEntry | undefined;
    onDragStart: (event: DragStartEvent) => void;
    onDragEnd: (event: DragEndEvent) => void;
};
declare const useDndLayers: (layers: Layer[], layerOrder: LayerOrder) => DndLayersHook;
export default useDndLayers;
