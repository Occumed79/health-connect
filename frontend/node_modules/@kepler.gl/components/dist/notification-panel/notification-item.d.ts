import React from 'react';
import { ActionHandler, removeNotification as removeNotificationActions } from '@kepler.gl/actions';
interface NotificationItemProps {
    notification: {
        id: string;
        type: string;
        message: string;
        count?: number;
    };
    isExpanded?: boolean;
    removeNotification?: ActionHandler<typeof removeNotificationActions>;
    theme?: any;
}
export default function NotificationItemFactory(): ({ notification, removeNotification, isExpanded: initialIsExpanded, theme }: NotificationItemProps) => React.JSX.Element;
export {};
