import { SimpleMeshLayer } from '@deck.gl/mesh-layers';
import type { GlobeConfig } from '@kepler.gl/constants';
/**
 * Convert a mapState zoom level to a [0, 1] atmosphere opacity multiplier.
 * Returns 1 (fully visible) below ATMOSPHERE_FADE_ZOOM_START, smoothly
 * interpolates to 0 between the two thresholds, and returns 0 above
 * ATMOSPHERE_FADE_ZOOM_END.
 */
export declare function atmosphereZoomFade(zoom: number): number;
type AtmosphereLayerProps = {
    config: GlobeConfig;
    /** Pre-computed [0, 1] zoom-fade multiplier; 1 = fully visible, 0 = hidden. */
    zoomFade: number;
};
export declare class AtmosphereLayerRealistic extends SimpleMeshLayer<any, AtmosphereLayerProps> {
    static layerName: string;
    draw({ uniforms }: {
        uniforms: object;
    }): void;
    getShaders(): any;
}
export declare class AtmosphereSkyLayerRealistic extends SimpleMeshLayer<any, AtmosphereLayerProps> {
    static layerName: string;
    draw({ uniforms }: {
        uniforms: object;
    }): void;
    getShaders(): any;
}
export declare const getGlobeAtmosphereLayer: ({ config, zoom }: {
    config: GlobeConfig;
    zoom?: number;
}) => AtmosphereLayerRealistic;
export declare const getGlobeAtmosphereSkyLayer: ({ config, zoom }: {
    config: GlobeConfig;
    zoom?: number;
}) => AtmosphereSkyLayerRealistic;
export {};
