import type { Texture } from '@luma.gl/core';
import type { ImageInput, ImageState } from './types';
/** luma.gl device interface for texture creation */
interface LumaDevice {
    createTexture(props: Record<string, unknown>): Texture;
    gl?: WebGL2RenderingContext;
}
type LoadImagesOptions = {
    gl: WebGL2RenderingContext;
    device?: LumaDevice;
    images: ImageState;
    imagesData: ImageInput;
    oldImagesData: ImageInput;
};
/**
 * Load image items to webgl context
 */
type LoadImagesResult = {
    /** Updated images state, or null if nothing changed */
    images: ImageState | null;
    /** True when at least one texture upload failed and should be retried */
    hasPendingUploads: boolean;
};
export declare function loadImages({ gl, device, images, imagesData, oldImagesData }: LoadImagesOptions): LoadImagesResult;
export {};
