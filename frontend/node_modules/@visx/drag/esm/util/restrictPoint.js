import clampNumber from "./clampNumber.js";
import getClosestPoint from "./getClosestPoint.js";
/** Restrict a point to an area, or samples along a path. */
export default function restrictPoint(point, samples) {
  let restrict = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : {};
  if (samples.length > 0) {
    return getClosestPoint(point, samples);
  }
  return {
    x: clampNumber(point.x, restrict.xMin ?? -Infinity, restrict.xMax ?? Infinity),
    y: clampNumber(point.y, restrict.yMin ?? -Infinity, restrict.yMax ?? Infinity)
  };
}