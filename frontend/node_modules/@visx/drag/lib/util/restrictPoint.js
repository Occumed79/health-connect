"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = restrictPoint;
var _clampNumber = _interopRequireDefault(require("./clampNumber"));
var _getClosestPoint = _interopRequireDefault(require("./getClosestPoint"));
function _interopRequireDefault(e) { return e && e.__esModule ? e : { default: e }; }
/** Restrict a point to an area, or samples along a path. */
function restrictPoint(point, samples) {
  let restrict = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : {};
  if (samples.length > 0) {
    return (0, _getClosestPoint.default)(point, samples);
  }
  return {
    x: (0, _clampNumber.default)(point.x, restrict.xMin ?? -Infinity, restrict.xMax ?? Infinity),
    y: (0, _clampNumber.default)(point.y, restrict.yMin ?? -Infinity, restrict.yMax ?? Infinity)
  };
}