/* eslint react/jsx-handler-names: 0 */
import { Component } from 'react';
import { Drag } from '@visx/drag';
import { getPageCoordinates } from "./utils.js";
import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
const DRAGGING_OVERLAY_STYLES = {
  cursor: 'move'
};
export default class BrushSelection extends Component {
  static defaultProps = {
    onMouseLeave: null,
    onMouseUp: null,
    onMouseMove: null,
    onClick: null
  };
  selectionDragStart = drag => {
    const {
      onMoveSelectionChange,
      onBrushStart
    } = this.props;
    if (onMoveSelectionChange) {
      onMoveSelectionChange('move', getPageCoordinates(drag.event));
    }
    if (onBrushStart) {
      onBrushStart(drag);
    }
  };
  selectionDragMove = drag => {
    const {
      updateBrush,
      isControlled
    } = this.props;
    if (isControlled) return;
    updateBrush(prevBrush => {
      const {
        x: x0,
        y: y0
      } = prevBrush.start;
      const {
        x: x1,
        y: y1
      } = prevBrush.end;
      const validDx = drag.dx > 0 ? Math.min(drag.dx, prevBrush.bounds.x1 - x1) : Math.max(drag.dx, prevBrush.bounds.x0 - x0);
      const validDy = drag.dy > 0 ? Math.min(drag.dy, prevBrush.bounds.y1 - y1) : Math.max(drag.dy, prevBrush.bounds.y0 - y0);
      return {
        ...prevBrush,
        isBrushing: true,
        extent: {
          ...prevBrush.extent,
          x0: x0 + validDx,
          x1: x1 + validDx,
          y0: y0 + validDy,
          y1: y1 + validDy
        }
      };
    });
  };
  selectionDragEnd = () => {
    const {
      updateBrush,
      onBrushEnd,
      onMoveSelectionChange,
      isControlled
    } = this.props;
    if (!isControlled) {
      updateBrush(prevBrush => {
        const nextBrush = {
          ...prevBrush,
          isBrushing: false,
          start: {
            ...prevBrush.start,
            x: Math.min(prevBrush.extent.x0, prevBrush.extent.x1),
            y: Math.min(prevBrush.extent.y0, prevBrush.extent.y1)
          },
          end: {
            ...prevBrush.end,
            x: Math.max(prevBrush.extent.x0, prevBrush.extent.x1),
            y: Math.max(prevBrush.extent.y0, prevBrush.extent.y1)
          }
        };
        if (onBrushEnd) {
          onBrushEnd(nextBrush);
        }
        return nextBrush;
      });
    }
    if (onMoveSelectionChange) {
      onMoveSelectionChange();
    }
  };
  render() {
    const {
      width,
      height,
      stageWidth,
      stageHeight,
      brush,
      disableDraggingSelection,
      onMouseLeave,
      onMouseMove,
      onMouseUp,
      onClick,
      selectedBoxStyle,
      isControlled,
      isDragInProgress
    } = this.props;
    return /*#__PURE__*/_jsx(Drag, {
      width: width,
      height: height,
      resetOnStart: true,
      onDragStart: this.selectionDragStart,
      onDragMove: this.selectionDragMove,
      onDragEnd: this.selectionDragEnd,
      isDragging: isControlled ? isDragInProgress : undefined,
      children: _ref => {
        let {
          isDragging,
          dragStart,
          dragEnd,
          dragMove
        } = _ref;
        return /*#__PURE__*/_jsxs("g", {
          children: [isDragging && /*#__PURE__*/_jsx("rect", {
            width: stageWidth,
            height: stageHeight,
            fill: "transparent",
            onPointerUp: isControlled ? undefined : dragEnd,
            onPointerMove: dragMove,
            onPointerLeave: isControlled ? undefined : dragEnd,
            style: DRAGGING_OVERLAY_STYLES
          }), /*#__PURE__*/_jsx("rect", {
            x: Math.min(brush.extent.x0, brush.extent.x1),
            y: Math.min(brush.extent.y0, brush.extent.y1),
            width: width,
            height: height,
            className: "visx-brush-selection",
            onPointerDown: disableDraggingSelection ? undefined : dragStart,
            onPointerLeave: event => {
              if (onMouseLeave) onMouseLeave(event);
            },
            onPointerMove: event => {
              dragMove(event);
              if (onMouseMove) onMouseMove(event);
            },
            onPointerUp: event => {
              if (!isControlled) {
                dragEnd(event);
              }
              if (onMouseUp) onMouseUp(event);
            },
            onClick: event => {
              if (onClick) onClick(event);
            },
            style: {
              pointerEvents: brush.isBrushing || brush.activeHandle ? 'none' : 'all',
              cursor: disableDraggingSelection ? undefined : 'move'
            },
            ...selectedBoxStyle
          })]
        });
      }
    });
  }
}