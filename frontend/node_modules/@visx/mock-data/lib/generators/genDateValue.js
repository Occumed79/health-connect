"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = genDateValue;
var _getSeededRandom = _interopRequireDefault(require("./getSeededRandom"));
function _interopRequireDefault(e) { return e && e.__esModule ? e : { default: e }; }
function genDateValue(length, /** Optional random seed in the interval [0, 1). */
seed, /** Optional start time in ms UTC. */
startTimeMs) {
  const random = seed == null ? Math.random : (0, _getSeededRandom.default)(seed);
  const startDateMs = startTimeMs == null ? Date.now() : new Date(startTimeMs).valueOf();
  return new Array(length).fill(1).map((_, idx) => ({
    date: new Date(startDateMs - idx * 3600000),
    // eslint-disable-next-line no-bitwise
    value: random() * 3000 | 0
  }));
}