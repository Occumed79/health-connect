import genBin from "./genBin.js";
export default function genBins(length, height, bin, count) {
  return new Array(length).fill(1).reduce((arr, _, i) => arr.concat([{
    bin: i,
    bins: genBin(height, bin, count)
  }]), []);
}