const defaultCount = (idx, number) => Math.random() * (25 * (number - idx));
const defaultBin = (idx, length) => idx * 150;
export default function genBin(length) {
  let bin = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : defaultBin;
  let count = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : defaultCount;
  return new Array(length).fill(1).reduce((data, d, i) => data.concat([{
    bin: bin(i, length),
    count: count(i, length)
  }]), []);
}