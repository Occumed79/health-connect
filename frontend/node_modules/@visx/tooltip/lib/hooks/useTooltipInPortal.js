"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = useTooltipInPortal;
var _react = require("react");
var _reactUseMeasure = _interopRequireDefault(require("react-use-measure"));
var _Portal = _interopRequireDefault(require("../Portal"));
var _Tooltip = _interopRequireDefault(require("../tooltips/Tooltip"));
var _TooltipWithBounds = _interopRequireDefault(require("../tooltips/TooltipWithBounds"));
var _jsxRuntime = require("react/jsx-runtime");
function _interopRequireDefault(e) { return e && e.__esModule ? e : { default: e }; }
/**
 * Hook that handles rendering of a Tooltip or TooltipWithBounds in a Portal.
 * Handles conversion of container coordinates to page coordinates using the container bounds.
 */
function useTooltipInPortal() {
  let {
    detectBounds: detectBoundsOption = true,
    zIndex: zIndexOption,
    ...useMeasureOptions
  } = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {};
  const [containerRef, containerBounds, forceRefreshBounds] = (0, _reactUseMeasure.default)(useMeasureOptions);
  const [isSsr, setIsSsr] = (0, _react.useState)(false);
  (0, _react.useEffect)(() => {
    setIsSsr(false);
  }, []);
  const TooltipInPortal = (0, _react.useMemo)(() => function (_ref) {
    let {
      left: containerLeft = 0,
      top: containerTop = 0,
      detectBounds: detectBoundsProp,
      // allow override at component-level
      zIndex: zIndexProp,
      // allow override at the component-level
      ...tooltipProps
    } = _ref;
    const detectBounds = detectBoundsProp == null ? detectBoundsOption : detectBoundsProp;
    const zIndex = zIndexProp == null ? zIndexOption : zIndexProp;
    const TooltipComponent = detectBounds ? _TooltipWithBounds.default : _Tooltip.default;
    // convert container coordinates to page coordinates
    const scrollX = isSsr ? 0 : window.scrollX;
    const scrollY = isSsr ? 0 : window.scrollY;
    const portalLeft = containerLeft + (containerBounds.left || 0) + scrollX;
    const portalTop = containerTop + (containerBounds.top || 0) + scrollY;
    return /*#__PURE__*/(0, _jsxRuntime.jsx)(_Portal.default, {
      zIndex: zIndex,
      children: /*#__PURE__*/(0, _jsxRuntime.jsx)(TooltipComponent, {
        left: portalLeft,
        top: portalTop,
        ...tooltipProps
      })
    });
  }, [detectBoundsOption, zIndexOption, containerBounds.left, containerBounds.top, isSsr]);
  return {
    containerRef,
    containerBounds,
    forceRefreshBounds,
    TooltipInPortal
  };
}