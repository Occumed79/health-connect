import { createElement } from 'react';
import cx from 'classnames';
import { Group } from '@visx/group';
import { tree as d3tree } from 'd3-hierarchy';
import DefaultLink from "../HierarchyDefaultLink.js";
import DefaultNode from "../HierarchyDefaultNode.js";
import { Fragment as _Fragment, jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
export default function Tree(_ref) {
  let {
    top,
    left,
    className,
    root,
    size,
    nodeSize,
    separation,
    children,
    linkComponent = DefaultLink,
    nodeComponent = DefaultNode
  } = _ref;
  const tree = d3tree();
  if (size) tree.size(size);
  if (nodeSize) tree.nodeSize(nodeSize);
  if (separation) tree.separation(separation);
  const data = tree(root);
  if (children) return /*#__PURE__*/_jsx(_Fragment, {
    children: children(data)
  });
  return /*#__PURE__*/_jsxs(Group, {
    top: top,
    left: left,
    className: cx('visx-tree', className),
    children: [linkComponent && data.links().map((link, i) => /*#__PURE__*/_jsx(Group, {
      children: /*#__PURE__*/createElement(linkComponent, {
        link
      })
    }, `tree-link-${i}`)), nodeComponent && data.descendants().map((node, i) => /*#__PURE__*/_jsx(Group, {
      children: /*#__PURE__*/createElement(nodeComponent, {
        node
      })
    }, `tree-node-${i}`))]
  });
}