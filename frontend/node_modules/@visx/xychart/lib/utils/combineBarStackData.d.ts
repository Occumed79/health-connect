import type { ReactElement } from 'react';
import type { AxisScale } from '@visx/axis';
import type { CombinedStackData, SeriesProps } from '../types';
/** Returns the value which forms a stack group. */
export declare const getStackValue: <XScale extends AxisScale, YScale extends AxisScale>(d: Pick<CombinedStackData<XScale, YScale>, "stack">) => import("@visx/scale").ScaleInput<XScale> | import("@visx/scale").ScaleInput<YScale>;
/**
 * Merges `seriesChildren` `props.data` by their `stack` value which
 * forms the stack grouping (`x` if vertical, `y` if horizontal)
 * and returns `CombinedStackData[]`.
 */
export default function combineBarStackData<XScale extends AxisScale, YScale extends AxisScale, Datum extends object>(seriesChildren: ReactElement<SeriesProps<XScale, YScale, Datum>>[], horizontal?: boolean): CombinedStackData<XScale, YScale>[];
//# sourceMappingURL=combineBarStackData.d.ts.map