"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = BaseAnnotation;
var _react = require("react");
var _scale = require("@visx/scale");
var _DataContext = _interopRequireDefault(require("../../../context/DataContext"));
var _getScaleBandwidth = _interopRequireDefault(require("../../../utils/getScaleBandwidth"));
var _isValidNumber = _interopRequireDefault(require("../../../typeguards/isValidNumber"));
var _jsxRuntime = require("react/jsx-runtime");
function _interopRequireDefault(e) { return e && e.__esModule ? e : { default: e }; }
// used for auto-positioning
const minimumLabelDimension = 16;
function BaseAnnotation(_ref) {
  let {
    AnnotationComponent,
    children,
    datum,
    dataKey,
    xAccessor: propsXAccessor,
    yAccessor: propsYAccessor,
    dx: propsDx = 0,
    dy: propsDy = 0,
    ...annotationProps
  } = _ref;
  const {
    innerHeight,
    innerWidth,
    margin,
    xScale,
    yScale,
    dataRegistry
  } = (0, _react.useContext)(_DataContext.default) || {};
  const xBandWidth = (0, _react.useMemo)(() => xScale ? (0, _getScaleBandwidth.default)(xScale) : 0, [xScale]);
  const yBandWidth = (0, _react.useMemo)(() => yScale ? (0, _getScaleBandwidth.default)(yScale) : 0, [yScale]);
  if ((!propsXAccessor || !propsYAccessor) && !dataKey) {
    console.warn('[@visx/xychart/BaseAnnotation]: dataKey or x/yAccessors must be specified.');
    return null;
  }
  const registryEntry = propsXAccessor && propsYAccessor || dataKey == null ? null : dataRegistry?.get(dataKey);
  const xAccessor = propsXAccessor || registryEntry?.xAccessor;
  const yAccessor = propsYAccessor || registryEntry?.yAccessor;
  if (!xScale || !yScale || !innerWidth || !innerHeight || !xAccessor || !yAccessor || !margin) {
    return null;
  }
  const x = ((0, _scale.coerceNumber)(xScale(xAccessor(datum))) ?? NaN) + xBandWidth / 2;
  const y = ((0, _scale.coerceNumber)(yScale(yAccessor(datum))) ?? NaN) + yBandWidth / 2;
  const dx = x + propsDx + minimumLabelDimension > margin.left + innerWidth ? -propsDx : propsDx;
  const dy = y + propsDy + minimumLabelDimension > margin.top + innerHeight ? -propsDy : propsDy;
  return (0, _isValidNumber.default)(x) && (0, _isValidNumber.default)(y) ? /*#__PURE__*/(0, _jsxRuntime.jsx)(AnnotationComponent, {
    width: innerWidth,
    height: innerHeight,
    ...annotationProps,
    x: x,
    y: y,
    dx: dx,
    dy: dy,
    children: children
  }) : null;
}