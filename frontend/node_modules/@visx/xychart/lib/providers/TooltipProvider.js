"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = TooltipProvider;
var _react = require("react");
var _responsive = require("@visx/responsive");
var _tooltip = require("@visx/tooltip");
var _TooltipContext = _interopRequireDefault(require("../context/TooltipContext"));
var _isValidNumber = _interopRequireDefault(require("../typeguards/isValidNumber"));
var _jsxRuntime = require("react/jsx-runtime");
function _interopRequireDefault(e) { return e && e.__esModule ? e : { default: e }; }
/** Simple wrapper around useTooltip, to provide tooltip data via context. */
function TooltipProvider(_ref) {
  let {
    hideTooltipDebounceMs = 400,
    children
  } = _ref;
  const {
    tooltipOpen,
    tooltipLeft,
    tooltipTop,
    tooltipData,
    updateTooltip,
    hideTooltip: privateHideTooltip
  } = (0, _tooltip.useTooltip)(undefined);
  const debouncedHideTooltip = (0, _react.useRef)(null);
  function cancelDeboundedHideTooltip() {
    if (debouncedHideTooltip.current) {
      debouncedHideTooltip.current.cancel();
      debouncedHideTooltip.current = null;
    }
  }
  (0, _react.useEffect)(() => cancelDeboundedHideTooltip, []);
  const showTooltip = (0, _react.useRef)(_ref2 => {
    let {
      svgPoint,
      index,
      key,
      datum,
      distanceX,
      distanceY
    } = _ref2;
    // cancel any hideTooltip calls so it won't hide after invoking the logic below
    cancelDeboundedHideTooltip();
    const cleanDistanceX = (0, _isValidNumber.default)(distanceX) ? distanceX : Infinity;
    const cleanDistanceY = (0, _isValidNumber.default)(distanceY) ? distanceY : Infinity;
    const distance = Math.sqrt(cleanDistanceX ** 2 + cleanDistanceY ** 2);
    updateTooltip(_ref3 => {
      let {
        tooltipData: currData
      } = _ref3;
      const currNearestDatumDistance = currData?.nearestDatum && (0, _isValidNumber.default)(currData.nearestDatum.distance) ? currData.nearestDatum.distance : Infinity;
      return {
        tooltipOpen: true,
        tooltipLeft: svgPoint?.x,
        tooltipTop: svgPoint?.y,
        tooltipData: {
          nearestDatum: (currData?.nearestDatum?.key ?? '') !== key && currNearestDatumDistance < distance ? currData?.nearestDatum : {
            key,
            index,
            datum,
            distance
          },
          datumByKey: {
            ...currData?.datumByKey,
            [key]: {
              datum,
              index,
              key
            }
          }
        }
      };
    });
  });
  const hideTooltip = (0, _react.useCallback)(() => {
    debouncedHideTooltip.current = (0, _responsive.debounce)(privateHideTooltip, hideTooltipDebounceMs);
    debouncedHideTooltip.current();
  }, [privateHideTooltip, hideTooltipDebounceMs]);
  const value = (0, _react.useMemo)(() => ({
    tooltipOpen,
    tooltipLeft,
    tooltipTop,
    tooltipData,
    updateTooltip,
    showTooltip: showTooltip.current,
    hideTooltip
  }), [hideTooltip, tooltipData, tooltipLeft, tooltipOpen, tooltipTop, updateTooltip]);
  return /*#__PURE__*/(0, _jsxRuntime.jsx)(_TooltipContext.default.Provider, {
    value: value,
    children: children
  });
}