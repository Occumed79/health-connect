import cx from 'classnames';
import { Fragment as _Fragment, jsx as _jsx } from "react/jsx-runtime";
export default function Polygon(_ref) {
  let {
    polygon,
    className,
    children,
    ...restProps
  } = _ref;
  if (!polygon) return null;
  const path = `M${polygon.join('L')}Z`;
  if (children) return /*#__PURE__*/_jsx(_Fragment, {
    children: children({
      path,
      polygon
    })
  });
  return /*#__PURE__*/_jsx("path", {
    className: cx('visx-delaunay-polygon', className),
    d: path,
    ...restProps
  });
}