import { ViewportType } from '../../../enums/index.js';
import type { DataAddOptions, LoadedData, RenderPathAttachment, RenderPathDefinition, RenderPath } from '../ViewportArchitectureTypes.js';
import type { PlanarDataPresentation, PlanarViewportRenderContext, PlanarVtkImageAdapterContext } from './PlanarViewportTypes.js';
export declare class VtkImageMapperRenderPath implements RenderPath<PlanarVtkImageAdapterContext> {
    addData(ctx: PlanarVtkImageAdapterContext, data: LoadedData, options: DataAddOptions): Promise<RenderPathAttachment<PlanarDataPresentation>>;
    private updateDataPresentation;
    private applyViewState;
    private canvasToWorld;
    private worldToCanvas;
    private getFrameOfReferenceUID;
    private getImageData;
    private removeData;
    private resize;
}
export declare class VtkImageMapperPath implements RenderPathDefinition<PlanarViewportRenderContext, PlanarVtkImageAdapterContext> {
    readonly id = "planar:vtk-image-mapper";
    readonly type = ViewportType.PLANAR_NEXT;
    matches(data: LoadedData, options: DataAddOptions): boolean;
    createRenderPath(): VtkImageMapperRenderPath;
    selectContext(rootContext: PlanarViewportRenderContext): PlanarVtkImageAdapterContext;
}
