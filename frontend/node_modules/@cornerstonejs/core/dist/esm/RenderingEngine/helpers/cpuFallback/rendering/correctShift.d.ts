import type { CPUFallbackViewport } from '../../../../types/index.js';
interface Shift {
    x: number;
    y: number;
}
export default function (shift: Shift, viewportOrientation: CPUFallbackViewport): Shift;
export {};
