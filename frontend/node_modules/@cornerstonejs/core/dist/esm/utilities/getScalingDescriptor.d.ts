import type { IViewport } from '../types/IViewport.js';
import type { ScalingDescriptor } from '../types/ScalingDescriptor.js';
type VolumeLike = {
    metadata?: {
        Modality?: string;
    };
    scaling?: object;
} | undefined;
declare function _getScalingDescriptor(viewport: IViewport, targetId?: string, getVolume?: (volumeId: string) => VolumeLike): ScalingDescriptor | undefined;
export { _getScalingDescriptor };
