import type { VolumeActor } from './IActor.js';
import type BlendModes from '../enums/BlendModes.js';
type VolumeInputCallback = (params: {
    volumeActor: VolumeActor;
    volumeId: string;
}) => unknown;
type IVolumeInput = {
    volumeId: string;
    actorUID?: string;
    visibility?: boolean;
    callback?: VolumeInputCallback;
    blendMode?: BlendModes;
    slabThickness?: number;
    [key: string]: unknown;
};
export type { IVolumeInput, VolumeInputCallback };
