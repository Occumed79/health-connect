/**
 * Copyright (c) 2026 mol* contributors, licensed under MIT, See LICENSE file for more info.
 *
 * @author David Sehnal <david.sehnal@gmail.com>
 */
import { DefaultPluginSpec } from '../../mol-plugin/spec.js';
import { PluginContext } from '../../mol-plugin/context.js';
import { SingleAsyncQueue } from '../../mol-util/single-async-queue.js';
export class PluginViewModel {
    get initialized() {
        return this.plugin.initialized;
    }
    async init() {
        await this.plugin.init();
    }
    mount(root) {
        this.mountQueue.enqueue(() => this.plugin.mountAsync(root));
    }
    unmount() {
        this.mountQueue.enqueue(() => this.plugin.unmount());
    }
    constructor(options) {
        var _a;
        this.mountQueue = new SingleAsyncQueue();
        const spec = (_a = options === null || options === void 0 ? void 0 : options.spec) !== null && _a !== void 0 ? _a : DefaultPluginSpec();
        this.plugin = new PluginContext(spec);
        this.init();
    }
}
