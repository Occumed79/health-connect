/**
 * Copyright (c) 2026 mol* contributors, licensed under MIT, See LICENSE file for more info.
 *
 * @author David Sehnal <david.sehnal@gmail.com>
 */
import { useRef } from 'react';
import { DefaultPluginUISpec } from '../../../mol-plugin-ui/spec.js';
import { PluginUIViewModel } from '../ui-view-model.js';
export function useCreatePluginUIViewModel(options) {
    const model = useRef();
    if (!model.current) {
        const spec = (options === null || options === void 0 ? void 0 : options.spec)
            ? (typeof options.spec === 'function' ? options.spec(DefaultPluginUISpec()) : options.spec)
            : DefaultPluginUISpec();
        model.current = (options === null || options === void 0 ? void 0 : options.model) ? options.model(spec) : new PluginUIViewModel({ spec });
    }
    return model.current;
}
