/**
 * Copyright (c) 2026 mol* contributors, licensed under MIT, See LICENSE file for more info.
 *
 * @author David Sehnal <david.sehnal@gmail.com>
 */
import { useEffect, useRef } from 'react';
import { DefaultPluginSpec } from '../../../mol-plugin/spec.js';
import { PluginViewModel } from '../view-model.js';
export function useCreatePluginViewModel(options) {
    const model = useRef();
    if (!model.current) {
        const spec = (options === null || options === void 0 ? void 0 : options.spec)
            ? (typeof options.spec === 'function' ? options.spec(DefaultPluginSpec()) : options.spec)
            : DefaultPluginSpec();
        model.current = (options === null || options === void 0 ? void 0 : options.model) ? options.model(spec) : new PluginViewModel({ spec });
    }
    return model.current;
}
export function usePluginViewModel(view, parent) {
    useEffect(() => {
        if (!parent.current)
            return;
        view.mount(parent.current);
        return () => view.unmount();
    }, [view]);
}
