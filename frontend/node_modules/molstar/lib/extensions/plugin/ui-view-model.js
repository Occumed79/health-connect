/**
 * Copyright (c) 2026 mol* contributors, licensed under MIT, See LICENSE file for more info.
 *
 * @author David Sehnal <david.sehnal@gmail.com>
 */
import { DefaultPluginUISpec } from '../../mol-plugin-ui/spec.js';
import { PluginUIContext } from '../../mol-plugin-ui/context.js';
export class PluginUIViewModel {
    get initialized() {
        return this.plugin.initialized;
    }
    async init() {
        await this.plugin.init();
    }
    constructor(options) {
        var _a;
        const spec = (_a = options === null || options === void 0 ? void 0 : options.spec) !== null && _a !== void 0 ? _a : DefaultPluginUISpec();
        this.plugin = new PluginUIContext(spec);
        this.init();
    }
}
