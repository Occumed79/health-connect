/**
 * Copyright (c) 2026 mol* contributors, licensed under MIT, See LICENSE file for more info.
 * @author Tadej Satler <tadej.satler@gmail.com>
 */
import { BehaviorSubject } from 'rxjs';
import { PluginContext } from '../../mol-plugin/context.js';
import { Volume } from '../../mol-model/volume.js';
import { Structure } from '../../mol-model/structure.js';
import { Color } from '../../mol-util/color/index.js';
import { StateTransform } from '../../mol-state/index.js';
import type { MaskCreatorState, MaskSource, ViewMask } from '../../extensions/volume-mask/types.js';
export declare const MASK_OVERLAY_COLOR: Color;
export declare class VolumeMaskController {
    private plugin;
    readonly state: BehaviorSubject<MaskCreatorState>;
    private highlightedChains?;
    private thresholdPreviewTimer;
    private thresholdPreviewInFlight;
    private thresholdPreviewSession;
    private pendingThresholdPreview;
    constructor(plugin: PluginContext);
    get current(): MaskCreatorState;
    private update;
    setTargetVolume(ref: StateTransform.Ref | undefined): void;
    setTargetStructure(ref: StateTransform.Ref | undefined): void;
    setSelectedChainIds(ids: string[]): void;
    toggleChainId(id: string): void;
    setDrawing(active: boolean): void;
    addViewMask(mask: ViewMask): void;
    removeViewMask(id: string): void;
    invertViewMask(id: string): void;
    setDilation(value: number): void;
    setSoftEdge(value: number): void;
    setMaskSource(value: MaskSource): void;
    setProteinRadius(value: number): void;
    setThreshold(value: Volume.IsoValue): void;
    /** Update threshold and schedule a coalesced live preview on the source isosurface. */
    previewThreshold(value: Volume.IsoValue): void;
    /** Immediately preview the latest threshold value on the source isosurface. */
    commitThresholdPreview(): void;
    private clearThresholdPreviewTimer;
    private clearPendingThresholdPreview;
    private flushThresholdPreview;
    private applyThresholdPreview;
    setVolumeOpacity(value: number): Promise<void>;
    toggleVolumeVisibility(): void;
    toggleStructureVisibility(): void;
    createMask(): Promise<void>;
    clearMask(): Promise<void>;
    invertMask(): Promise<void>;
    applyMaskToVolume(): Promise<void>;
    flipHandedness(): Promise<void>;
    removeDust(minVoxels: number): Promise<void>;
    private finalizeVolumeEdit;
    saveMask(): void;
    saveVolume(): void;
    dispose(): void;
    private clearSelectedChainHighlight;
    private syncSelectedChainHighlight;
}
export declare function getChainIds(structure: Structure): string[];
