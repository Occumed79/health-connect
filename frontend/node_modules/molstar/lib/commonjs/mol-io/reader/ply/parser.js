"use strict";
/**
 * Copyright (c) 2019-2026 mol* contributors, licensed under MIT, See LICENSE file for more info.
 *
 * @author Alexander Rose <alexander.rose@weirdbyte.de>
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.parsePly = parsePly;
const result_1 = require("../result.js");
const mol_task_1 = require("../../../mol-task/index.js");
const schema_1 = require("./schema.js");
const tokenizer_1 = require("../common/text/tokenizer.js");
const db_1 = require("../../../mol-data/db.js");
const token_1 = require("../common/text/column/token.js");
const utf8_1 = require("../../common/utf8.js");
function State(data, runtimeCtx, presetFormat) {
    const tokenizer = (0, tokenizer_1.Tokenizer)(data);
    return {
        data,
        tokenizer,
        runtimeCtx,
        format: presetFormat !== null && presetFormat !== void 0 ? presetFormat : 'ascii',
        formatPreset: presetFormat !== undefined,
        littleEndian: presetFormat !== 'binary_big_endian',
        binaryData: undefined,
        binaryOffset: 0,
        comments: [],
        elementSpecs: [],
        elements: []
    };
}
function markHeader(tokenizer) {
    const endHeaderIndex = tokenizer.data.indexOf('end_header', tokenizer.position);
    if (endHeaderIndex === -1)
        throw new Error(`no 'end_header' record found`);
    // TODO set `tokenizer.lineNumber` correctly
    tokenizer.tokenStart = tokenizer.position;
    tokenizer.tokenEnd = endHeaderIndex;
    tokenizer.position = endHeaderIndex;
    tokenizer_1.Tokenizer.eatLine(tokenizer);
}
function parseHeader(state) {
    const { tokenizer, comments, elementSpecs } = state;
    markHeader(tokenizer);
    const headerLines = tokenizer_1.Tokenizer.getTokenString(tokenizer).split(/\r?\n/);
    if (headerLines[0] !== 'ply')
        throw new Error(`data not starting with 'ply'`);
    if (!state.formatPreset) {
        const formatLine = headerLines[1];
        if (formatLine === 'format ascii 1.0') {
            state.format = 'ascii';
            state.littleEndian = true;
        }
        else if (formatLine === 'format binary_little_endian 1.0') {
            state.format = 'binary_little_endian';
            state.littleEndian = true;
        }
        else if (formatLine === 'format binary_big_endian 1.0') {
            state.format = 'binary_big_endian';
            state.littleEndian = false;
        }
        else {
            throw new Error(`unsupported PLY format '${formatLine}'`);
        }
    }
    let currentName;
    let currentCount;
    let currentProperties;
    function addCurrentElementSchema() {
        if (currentName !== undefined && currentCount !== undefined && currentProperties !== undefined) {
            let isList = false;
            for (let i = 0, il = currentProperties.length; i < il; ++i) {
                const p = currentProperties[i];
                if (p.kind === 'list') {
                    isList = true;
                    break;
                }
            }
            if (isList && currentProperties.length !== 1) {
                // TODO handle lists with appended properties
                //      currently only the list part will be accessible
            }
            if (isList) {
                elementSpecs.push({
                    kind: 'list',
                    name: currentName,
                    count: currentCount,
                    property: currentProperties[0]
                });
            }
            else {
                elementSpecs.push({
                    kind: 'table',
                    name: currentName,
                    count: currentCount,
                    properties: currentProperties
                });
            }
        }
    }
    for (let i = 2, il = headerLines.length; i < il; ++i) {
        const l = headerLines[i];
        const ls = l.split(' ');
        if (l.startsWith('comment')) {
            comments.push(l.substr(8));
        }
        else if (l.startsWith('element')) {
            addCurrentElementSchema();
            currentProperties = [];
            currentName = ls[1];
            currentCount = parseInt(ls[2]);
        }
        else if (l.startsWith('property')) {
            if (currentProperties === undefined)
                throw new Error(`properties outside of element`);
            if (ls[1] === 'list') {
                currentProperties.push({
                    kind: 'list',
                    countType: (0, schema_1.PlyType)(ls[2]),
                    dataType: (0, schema_1.PlyType)(ls[3]),
                    name: ls[4]
                });
            }
            else {
                currentProperties.push({
                    kind: 'column',
                    type: (0, schema_1.PlyType)(ls[1]),
                    name: ls[2]
                });
            }
        }
        else if (l.startsWith('end_header')) {
            addCurrentElementSchema();
        }
        else {
            console.warn('unknown header line');
        }
    }
}
function parseElements(state) {
    const { elementSpecs } = state;
    if (state.format === 'ascii') {
        for (let i = 0, il = elementSpecs.length; i < il; ++i) {
            const spec = elementSpecs[i];
            if (spec.kind === 'table')
                parseTableElement(state, spec);
            else if (spec.kind === 'list')
                parseListElement(state, spec);
        }
    }
    else {
        for (let i = 0, il = elementSpecs.length; i < il; ++i) {
            const spec = elementSpecs[i];
            if (spec.kind === 'table')
                parseBinaryTableElement(state, spec);
            else if (spec.kind === 'list')
                parseBinaryListElement(state, spec);
        }
    }
}
function getColumnSchema(type) {
    switch (type) {
        case 'char':
        case 'uchar':
        case 'int8':
        case 'uint8':
        case 'short':
        case 'ushort':
        case 'int16':
        case 'uint16':
        case 'int':
        case 'uint':
        case 'int32':
        case 'uint32':
            return db_1.Column.Schema.int;
        case 'float':
        case 'double':
        case 'float32':
        case 'float64':
            return db_1.Column.Schema.float;
    }
}
function readBinaryValue(dv, offset, type, littleEndian) {
    switch (type) {
        case 'char':
        case 'int8': return dv.getInt8(offset);
        case 'uchar':
        case 'uint8': return dv.getUint8(offset);
        case 'short':
        case 'int16': return dv.getInt16(offset, littleEndian);
        case 'ushort':
        case 'uint16': return dv.getUint16(offset, littleEndian);
        case 'int':
        case 'int32': return dv.getInt32(offset, littleEndian);
        case 'uint':
        case 'uint32': return dv.getUint32(offset, littleEndian);
        case 'float':
        case 'float32': return dv.getFloat32(offset, littleEndian);
        case 'double':
        case 'float64': return dv.getFloat64(offset, littleEndian);
    }
}
function makeTypedArray(type, count) {
    switch (type) {
        case 'char':
        case 'int8': return new Int8Array(count);
        case 'uchar':
        case 'uint8': return new Uint8Array(count);
        case 'short':
        case 'int16': return new Int16Array(count);
        case 'ushort':
        case 'uint16': return new Uint16Array(count);
        case 'int':
        case 'int32': return new Int32Array(count);
        case 'uint':
        case 'uint32': return new Uint32Array(count);
        case 'float':
        case 'float32': return new Float32Array(count);
        case 'double':
        case 'float64': return new Float64Array(count);
    }
}
function parseBinaryTableElement(state, spec) {
    const { elements, binaryData, littleEndian } = state;
    const { count, properties } = spec;
    const propertyCount = properties.length;
    const propertyNames = [];
    const propertyTypes = [];
    const propertyColumns = new Map();
    const arrays = [];
    for (let i = 0; i < propertyCount; i++)
        arrays.push(makeTypedArray(properties[i].type, count));
    const dv = new DataView(binaryData.buffer, binaryData.byteOffset);
    for (let i = 0; i < count; i++) {
        for (let j = 0; j < propertyCount; j++) {
            const { type } = properties[j];
            arrays[j][i] = readBinaryValue(dv, state.binaryOffset, type, littleEndian);
            state.binaryOffset += schema_1.PlyTypeByteLength[type];
        }
    }
    for (let i = 0; i < propertyCount; i++) {
        const { type, name } = properties[i];
        const schema = getColumnSchema(type);
        const column = db_1.Column.ofArray({ array: arrays[i], schema });
        propertyNames.push(name);
        propertyTypes.push(type);
        propertyColumns.set(name, column);
    }
    elements.push({
        kind: 'table',
        rowCount: count,
        propertyNames,
        propertyTypes,
        getProperty: (name) => propertyColumns.get(name)
    });
}
function parseBinaryListElement(state, spec) {
    const { elements, binaryData, littleEndian } = state;
    const { count, property } = spec;
    const { countType, dataType } = property;
    const dv = new DataView(binaryData.buffer, binaryData.byteOffset);
    const allEntries = [];
    const offsets = new Uint32Array(count + 1);
    for (let i = 0; i < count; i++) {
        const listCount = readBinaryValue(dv, state.binaryOffset, countType, littleEndian);
        state.binaryOffset += schema_1.PlyTypeByteLength[countType];
        offsets[i + 1] = offsets[i] + listCount;
        for (let k = 0; k < listCount; k++) {
            allEntries.push(readBinaryValue(dv, state.binaryOffset, dataType, littleEndian));
            state.binaryOffset += schema_1.PlyTypeByteLength[dataType];
        }
    }
    /** holds row value entries transiently */
    const listValue = {
        entries: [],
        count: 0
    };
    elements.push({
        kind: 'list',
        rowCount: count,
        name: property.name,
        type: property.dataType,
        value: (row) => {
            const start = offsets[row];
            const end = offsets[row + 1];
            listValue.count = end - start;
            for (let i = start; i < end; i++) {
                listValue.entries[i - start] = allEntries[i];
            }
            return listValue;
        }
    });
}
function parseTableElement(state, spec) {
    const { elements, tokenizer } = state;
    const { count, properties } = spec;
    const propertyCount = properties.length;
    const propertyNames = [];
    const propertyTypes = [];
    const propertyTokens = [];
    const propertyColumns = new Map();
    for (let i = 0, il = propertyCount; i < il; ++i) {
        const tokens = tokenizer_1.TokenBuilder.create(tokenizer.data, count * 2);
        propertyTokens.push(tokens);
    }
    for (let i = 0, il = count; i < il; ++i) {
        for (let j = 0, jl = propertyCount; j < jl; ++j) {
            tokenizer_1.Tokenizer.skipWhitespace(tokenizer);
            tokenizer_1.Tokenizer.markStart(tokenizer);
            tokenizer_1.Tokenizer.eatValue(tokenizer);
            tokenizer_1.TokenBuilder.addUnchecked(propertyTokens[j], tokenizer.tokenStart, tokenizer.tokenEnd);
        }
    }
    for (let i = 0, il = propertyCount; i < il; ++i) {
        const { type, name } = properties[i];
        const column = (0, token_1.TokenColumn)(propertyTokens[i], getColumnSchema(type));
        propertyNames.push(name);
        propertyTypes.push(type);
        propertyColumns.set(name, column);
    }
    elements.push({
        kind: 'table',
        rowCount: count,
        propertyNames,
        propertyTypes,
        getProperty: (name) => propertyColumns.get(name)
    });
}
function parseListElement(state, spec) {
    const { elements, tokenizer } = state;
    const { count, property } = spec;
    // initial tokens size assumes triangle index data
    const tokens = tokenizer_1.TokenBuilder.create(tokenizer.data, count * 2 * 3);
    const offsets = new Uint32Array(count + 1);
    let entryCount = 0;
    for (let i = 0, il = count; i < il; ++i) {
        tokenizer_1.Tokenizer.skipWhitespace(tokenizer);
        tokenizer_1.Tokenizer.markStart(tokenizer);
        while (tokenizer_1.Tokenizer.skipWhitespace(tokenizer) !== 10) {
            ++entryCount;
            tokenizer_1.Tokenizer.markStart(tokenizer);
            tokenizer_1.Tokenizer.eatValue(tokenizer);
            tokenizer_1.TokenBuilder.addToken(tokens, tokenizer);
        }
        offsets[i + 1] = entryCount;
    }
    /** holds row value entries transiently */
    const listValue = {
        entries: [],
        count: 0
    };
    const column = (0, token_1.TokenColumn)(tokens, getColumnSchema(property.dataType));
    elements.push({
        kind: 'list',
        rowCount: count,
        name: property.name,
        type: property.dataType,
        value: (row) => {
            const offset = offsets[row] + 1;
            const count = column.value(offset - 1);
            for (let i = offset, il = offset + count; i < il; ++i) {
                listValue.entries[i - offset] = column.value(i);
            }
            listValue.count = count;
            return listValue;
        }
    });
}
async function parseInternal(data, ctx) {
    let stateData;
    let binaryData;
    let binaryOffset = 0;
    let presetFormat;
    if (data instanceof Uint8Array) {
        binaryOffset = findEndHeaderByteOffset(data);
        const headerText = (0, utf8_1.utf8Read)(data, 0, binaryOffset);
        const format = detectPlyFormat(headerText);
        presetFormat = format;
        if (format !== 'ascii') {
            stateData = headerText;
            binaryData = data;
        }
        else {
            // ASCII PLY: decode the full byte array as text
            stateData = (0, utf8_1.utf8ReadLong)(data, 0, data.length);
        }
    }
    else {
        stateData = data;
    }
    const state = State(stateData, ctx, presetFormat);
    if (binaryData !== undefined) {
        state.binaryData = binaryData;
        state.binaryOffset = binaryOffset;
    }
    ctx.update({ message: 'Parsing...', current: 0, max: data.length });
    parseHeader(state);
    parseElements(state);
    const { elements, elementSpecs, comments } = state;
    const elementNames = elementSpecs.map(s => s.name);
    const result = (0, schema_1.PlyFile)(elements, elementNames, comments);
    return result_1.ReaderResult.success(result);
}
/** Extracts the PLY format from the header text (second line). */
function detectPlyFormat(headerText) {
    const newline = headerText.indexOf('\n');
    const formatLine = newline === -1 ? '' : headerText.substring(newline + 1, headerText.indexOf('\n', newline + 1)).replace(/\r$/, '');
    if (formatLine === 'format binary_little_endian 1.0')
        return 'binary_little_endian';
    if (formatLine === 'format binary_big_endian 1.0')
        return 'binary_big_endian';
    return 'ascii';
}
/** Returns the byte offset of the first byte after the `end_header\n` line. */
function findEndHeaderByteOffset(data) {
    const target = [101, 110, 100, 95, 104, 101, 97, 100, 101, 114]; // 'end_header'
    const tlen = target.length;
    for (let i = 0; i <= data.length - tlen; i++) {
        // Only match at the start of a line
        if (i !== 0 && data[i - 1] !== 10 /* \n */)
            continue;
        let matched = true;
        for (let j = 0; j < tlen; j++) {
            if (data[i + j] !== target[j]) {
                matched = false;
                break;
            }
        }
        if (!matched)
            continue;
        let k = i + tlen;
        if (k < data.length && data[k] === 13)
            k++; // skip \r
        if (k < data.length && data[k] === 10)
            k++; // skip \n
        return k;
    }
    throw new Error(`no 'end_header' record found`);
}
function parsePly(data) {
    return mol_task_1.Task.create('Parse PLY', async (ctx) => {
        return await parseInternal(data, ctx);
    });
}
