"use strict";
/**
 * Copyright (c) 2026 mol* contributors, licensed under MIT, See LICENSE file for more info.
 *
 * @author Alexander Rose <alexander.rose@weirdbyte.de>
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.SfFormat = void 0;
exports.volumeFromStructureFactors = volumeFromStructureFactors;
const mol_task_1 = require("../../mol-task/index.js");
const geometry_1 = require("../../mol-math/geometry.js");
const linear_algebra_1 = require("../../mol-math/linear-algebra.js");
const custom_property_1 = require("../../mol-model/custom-property.js");
const misc_1 = require("../../mol-math/misc.js");
const reflections_1 = require("./shared/reflections.js");
function volumeFromStructureFactors(source, params) {
    return mol_task_1.Task.create('Compute Structure Factor Map', async (ctx) => {
        var _a;
        const mapType = (_a = params === null || params === void 0 ? void 0 : params.mapType) !== null && _a !== void 0 ? _a : '2fo-fc';
        // 1. Parse cell parameters
        const { cell, symmetry, refln } = source;
        const spaceGroupId = symmetry.Int_Tables_number.value(0) ||
            symmetry['space_group_name_H-M'].value(0) ||
            'P 1';
        const cellSize = linear_algebra_1.Vec3.create(cell.length_a.value(0), cell.length_b.value(0), cell.length_c.value(0));
        const cellAngles = linear_algebra_1.Vec3.create((0, misc_1.degToRad)(cell.angle_alpha.value(0)), (0, misc_1.degToRad)(cell.angle_beta.value(0)), (0, misc_1.degToRad)(cell.angle_gamma.value(0)));
        const sgCell = geometry_1.SpacegroupCell.create(spaceGroupId, cellSize, cellAngles);
        const sg = geometry_1.Spacegroup.create(sgCell);
        // 2. Extract reflection arrays
        await ctx.update({ message: 'Reading reflections...' });
        const count = refln.index_h.rowCount;
        if (count === 0)
            throw new Error('No reflection data found in the _refln category.');
        const hArr = refln.index_h.toArray({ array: Int16Array });
        const kArr = refln.index_k.toArray({ array: Int16Array });
        const lArr = refln.index_l.toArray({ array: Int16Array });
        const ampColumn = mapType === '2fo-fc' ? refln.pdbx_FWT : refln.pdbx_DELFWT;
        const phiColumn = mapType === '2fo-fc' ? refln.pdbx_PHWT : refln.pdbx_DELPHWT;
        if (!ampColumn.isDefined || !phiColumn.isDefined) {
            const ampName = mapType === '2fo-fc' ? 'pdbx_FWT' : 'pdbx_DELFWT';
            const phiName = mapType === '2fo-fc' ? 'pdbx_PHWT' : 'pdbx_DELPHWT';
            throw new Error(`Structure factor CIF is missing required _refln.${ampName}/_refln.${phiName} map coefficients.`);
        }
        const ampCol = ampColumn.toArray({ array: Float32Array });
        const phiCol = phiColumn.toArray({ array: Float32Array });
        // 3–5. Compute density via shared helper
        const { density, N0, N1, N2 } = await (0, reflections_1.computeElectronDensityFromReflections)(hArr, kArr, lArr, ampCol, phiCol, sg, ctx);
        // 6. Compute statistics
        await ctx.update({ message: 'Finalizing volume...' });
        const totalSize = N0 * N1 * N2;
        let min = Infinity, max = -Infinity, sum = 0, sum2 = 0;
        for (let i = 0; i < totalSize; i++) {
            const v = density[i];
            if (v < min)
                min = v;
            if (v > max)
                max = v;
            sum += v;
            sum2 += v * v;
        }
        const mean = sum / totalSize;
        const sigma = Math.sqrt(Math.max(0, sum2 / totalSize - mean * mean));
        // 7. Create Tensor and Volume
        // axisOrderSlowToFast = [0, 1, 2]: axis 0 is slowest (h), axis 2 is fastest (l)
        const tensorSpace = linear_algebra_1.Tensor.Space([N0, N1, N2], [0, 1, 2], Float32Array);
        const data = linear_algebra_1.Tensor.create(tensorSpace, linear_algebra_1.Tensor.Data1(density));
        return {
            label: params === null || params === void 0 ? void 0 : params.label,
            entryId: params === null || params === void 0 ? void 0 : params.entryId,
            grid: {
                transform: {
                    kind: 'spacegroup',
                    cell: sgCell,
                    // The FFT grid covers the full unit cell [0, 1) in each direction
                    fractionalBox: geometry_1.Box3D.create(linear_algebra_1.Vec3.zero(), linear_algebra_1.Vec3.create(1, 1, 1)),
                },
                cells: data,
                stats: { min, max, mean, sigma },
                periodicity: 'xyz',
            },
            instances: [{ transform: linear_algebra_1.Mat4.identity() }],
            sourceData: SfFormat.create(source),
            customProperties: new custom_property_1.CustomProperties(),
            _propertyData: Object.create(null),
            _localPropertyData: Object.create(null),
        };
    });
}
var SfFormat;
(function (SfFormat) {
    function is(x) {
        return (x === null || x === void 0 ? void 0 : x.kind) === 'sf';
    }
    SfFormat.is = is;
    function create(db) {
        return { kind: 'sf', name: db._name, data: db };
    }
    SfFormat.create = create;
})(SfFormat || (exports.SfFormat = SfFormat = {}));
