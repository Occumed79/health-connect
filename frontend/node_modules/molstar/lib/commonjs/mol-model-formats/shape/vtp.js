"use strict";
/**
 * Copyright (c) 2026 mol* contributors, licensed under MIT, See LICENSE file for more info.
 *
 * @author Ludovic Autin <autin@scripps.edu>
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.VtpShapeParams = void 0;
exports.createVtpShapeParams = createVtpShapeParams;
exports.shapeFromVtp = shapeFromVtp;
const mol_task_1 = require("../../mol-task/index.js");
const mesh_builder_1 = require("../../mol-geo/geometry/mesh/mesh-builder.js");
const mesh_1 = require("../../mol-geo/geometry/mesh/mesh.js");
const shape_1 = require("../../mol-model/shape.js");
const util_1 = require("../../mol-data/util.js");
const param_definition_1 = require("../../mol-util/param-definition.js");
const names_1 = require("../../mol-util/color/names.js");
const scale_1 = require("../../mol-util/color/scale.js");
const lists_1 = require("../../mol-util/color/lists.js");
const value_cell_1 = require("../../mol-util/value-cell.js");
const object_1 = require("../../mol-util/object.js");
const mat4_1 = require("../../mol-math/linear-algebra/3d/mat4.js");
const _identityTransforms = [mat4_1.Mat4.identity()];
// --- Params ---
function fmtStat(x) {
    if (!isFinite(x))
        return x.toString();
    const abs = Math.abs(x);
    if (abs === 0)
        return '0';
    if (abs < 0.001 || abs >= 1e5)
        return x.toExponential(2);
    return x.toPrecision(3);
}
function computeAttrStatsText(vtpFile, attrKey) {
    let rawVals;
    let nComp = 1;
    let name = '';
    if (attrKey.startsWith('point:')) {
        name = attrKey.slice(6);
        const arr = vtpFile.pointData.get(name);
        if (!arr)
            return '';
        rawVals = arr.values.toArray();
        nComp = arr.numberOfComponents;
    }
    else if (attrKey.startsWith('cell:')) {
        name = attrKey.slice(5);
        const arr = vtpFile.cellData.get(name);
        if (!arr)
            return '';
        rawVals = arr.values.toArray();
        nComp = arr.numberOfComponents;
    }
    if (!rawVals)
        return '';
    const n = rawVals.length / nComp;
    let min = Infinity, max = -Infinity, sum = 0, sumSq = 0;
    for (let i = 0; i < n; i++) {
        let v;
        if (nComp === 1) {
            v = rawVals[i];
        }
        else {
            let mag2 = 0;
            for (let c = 0; c < nComp; c++)
                mag2 += rawVals[i * nComp + c] ** 2;
            v = Math.sqrt(mag2);
        }
        if (v < min)
            min = v;
        if (v > max)
            max = v;
        sum += v;
        sumSq += v * v;
    }
    const mean = sum / n;
    const std = Math.sqrt(Math.max(0, sumSq / n - mean * mean));
    const typeStr = nComp > 1 ? `${nComp}-comp magnitude` : 'scalar';
    return `${name}  (${typeStr}, n=${n})\nmin: ${fmtStat(min)}   max: ${fmtStat(max)}\nmean: ${fmtStat(mean)}   σ: ${fmtStat(std)}`;
}
function buildAttrOptions(vtpFile) {
    if (!vtpFile)
        return [['', 'No attributes']];
    const opts = [];
    for (const [name, arr] of vtpFile.pointData) {
        const nComp = arr.numberOfComponents;
        opts.push([`point:${name}`, nComp > 1 ? `Point: ${name} (mag)` : `Point: ${name}`]);
    }
    for (const [name, arr] of vtpFile.cellData) {
        const nComp = arr.numberOfComponents;
        opts.push([`cell:${name}`, nComp > 1 ? `Cell: ${name} (mag)` : `Cell: ${name}`]);
    }
    return opts.length > 0 ? opts : [['', 'No attributes']];
}
function defaultAttrKey(vtpFile) {
    if (!vtpFile)
        return '';
    // Prefer 1-component arrays first (direct scalar mapping)
    for (const [name, arr] of vtpFile.cellData) {
        if (arr.numberOfComponents === 1)
            return `cell:${name}`;
    }
    for (const [name, arr] of vtpFile.pointData) {
        if (arr.numberOfComponents === 1)
            return `point:${name}`;
    }
    if (vtpFile.cellData.size > 0)
        return `cell:${vtpFile.cellData.keys().next().value}`;
    if (vtpFile.pointData.size > 0)
        return `point:${vtpFile.pointData.keys().next().value}`;
    return '';
}
function scalarRange(values) {
    let min = Infinity, max = -Infinity;
    for (let i = 0; i < values.length; i++) {
        if (values[i] < min)
            min = values[i];
        if (values[i] > max)
            max = values[i];
    }
    return [min === Infinity ? 0 : min, max === -Infinity ? 1 : max];
}
function createVtpShapeParams(vtpFile, getStats) {
    var _a, _b;
    const attrOptions = buildAttrOptions(vtpFile);
    const defKey = defaultAttrKey(vtpFile);
    const defStats = vtpFile && defKey ? computeAttrStatsText(vtpFile, defKey) : '';
    const hasAttrs = ((_a = vtpFile === null || vtpFile === void 0 ? void 0 : vtpFile.pointData.size) !== null && _a !== void 0 ? _a : 0) + ((_b = vtpFile === null || vtpFile === void 0 ? void 0 : vtpFile.cellData.size) !== null && _b !== void 0 ? _b : 0) > 0;
    return {
        ...mesh_1.Mesh.Params,
        doubleSided: { ...mesh_1.Mesh.Params.doubleSided, defaultValue: true },
        interior: { ...mesh_1.Mesh.Params.interior, defaultValue: { ...mesh_1.Mesh.Params.interior.defaultValue, colorStrength: 0 } },
        colorTheme: param_definition_1.ParamDefinition.MappedStatic(hasAttrs ? 'attribute' : 'uniform', {
            attribute: param_definition_1.ParamDefinition.Group({
                name: param_definition_1.ParamDefinition.Select(defKey, attrOptions, { help: () => {
                        return { description: getStats ? getStats() : defStats };
                    } }),
                colors: param_definition_1.ParamDefinition.Select('viridis', lists_1.ColorListOptionsScale),
                domain: param_definition_1.ParamDefinition.MappedStatic('auto', {
                    custom: param_definition_1.ParamDefinition.Interval([-1, 1], { step: 0.001 }),
                    auto: param_definition_1.ParamDefinition.Group({
                        symmetric: param_definition_1.ParamDefinition.Boolean(false, { description: 'If true the automatic range is determined as [-|max|, |max|].' })
                    })
                })
            }),
            uniform: param_definition_1.ParamDefinition.Group({
                color: param_definition_1.ParamDefinition.Color(names_1.ColorNames.grey, { label: 'Uniform Color' })
            })
        }, { isEssential: true }),
        scale: param_definition_1.ParamDefinition.Numeric(1, { min: 0.01, max: 100, step: 0.01 }, { label: 'Scale', isEssential: true, description: 'Uniform scale factor applied to the mesh.' }),
    };
}
exports.VtpShapeParams = createVtpShapeParams();
// --- Mesh building ---
async function buildMesh(ctx, vtpFile, mesh) {
    const { positions, connectivity, numberOfPoints, numberOfTriangles } = vtpFile;
    const builderState = mesh_builder_1.MeshBuilder.createState(numberOfPoints, numberOfTriangles, mesh);
    const { vertices, indices, groups } = builderState;
    const chunkSize = 50000;
    for (let i = 0, il = numberOfPoints; i < il; i += chunkSize) {
        const end = Math.min(i + chunkSize, il);
        for (let v = i; v < end; v++) {
            util_1.ChunkedArray.add3(vertices, positions[3 * v], positions[3 * v + 1], positions[3 * v + 2]);
            util_1.ChunkedArray.add(groups, v);
        }
        if (ctx.shouldUpdate) {
            await ctx.update({ message: 'Building VTP mesh...', current: i, max: numberOfPoints + numberOfTriangles });
        }
    }
    for (let i = 0, il = numberOfTriangles; i < il; i += chunkSize) {
        const end = Math.min(i + chunkSize, il);
        for (let t = i; t < end; t++) {
            util_1.ChunkedArray.add3(indices, connectivity[3 * t], connectivity[3 * t + 1], connectivity[3 * t + 2]);
        }
        if (ctx.shouldUpdate) {
            await ctx.update({ message: 'Building VTP mesh...', current: numberOfPoints + i, max: numberOfPoints + numberOfTriangles });
        }
    }
    const m = mesh_builder_1.MeshBuilder.getMesh(builderState);
    // Requires consistent CCW winding. Files with alternating CW/CCW faces
    // (e.g. some lat/lon sphere exports) will show pinwheel shading artifacts.
    mesh_1.Mesh.computeNormals(m);
    value_cell_1.ValueCell.updateIfChanged(m.varyingGroup, true);
    return m;
}
// --- Color lookup ---
function cellToVertexAverage(vtpFile, cellValues) {
    const { connectivity, triangleCellIndex, numberOfPoints } = vtpFile;
    const nTris = connectivity.length / 3;
    const sum = new Float64Array(numberOfPoints);
    const count = new Uint32Array(numberOfPoints);
    for (let i = 0; i < nTris; i++) {
        const val = cellValues[triangleCellIndex[i]];
        const v0 = connectivity[3 * i];
        const v1 = connectivity[3 * i + 1];
        const v2 = connectivity[3 * i + 2];
        sum[v0] += val;
        count[v0]++;
        sum[v1] += val;
        count[v1]++;
        sum[v2] += val;
        count[v2]++;
    }
    const smoothed = new Float64Array(numberOfPoints);
    for (let v = 0; v < numberOfPoints; v++) {
        smoothed[v] = count[v] > 0 ? sum[v] / count[v] : 0;
    }
    return smoothed;
}
/**
 * Average multi-component cell vectors to vertices, then return the magnitude of each vertex vector.
 * Matching smgui _cell_to_vertex_interpolation_vector: averaging direction vectors, not their magnitudes,
 * so that crease/boundary vertices (where adjacent face normals cancel) get low magnitude values.
 */
function cellToVertexAverageMag(vtpFile, cellVectors, nComp) {
    const { connectivity, triangleCellIndex, numberOfPoints } = vtpFile;
    const nTris = connectivity.length / 3;
    const sum = new Float64Array(numberOfPoints * nComp);
    const count = new Uint32Array(numberOfPoints);
    for (let i = 0; i < nTris; i++) {
        const ci = triangleCellIndex[i];
        const v0 = connectivity[3 * i];
        const v1 = connectivity[3 * i + 1];
        const v2 = connectivity[3 * i + 2];
        for (let c = 0; c < nComp; c++) {
            const val = cellVectors[ci * nComp + c];
            sum[v0 * nComp + c] += val;
            sum[v1 * nComp + c] += val;
            sum[v2 * nComp + c] += val;
        }
        count[v0]++;
        count[v1]++;
        count[v2]++;
    }
    const result = new Float64Array(numberOfPoints);
    for (let v = 0; v < numberOfPoints; v++) {
        if (count[v] > 0) {
            let mag2 = 0;
            for (let c = 0; c < nComp; c++) {
                const avg = sum[v * nComp + c] / count[v];
                mag2 += avg * avg;
            }
            result[v] = Math.sqrt(mag2);
        }
    }
    return result;
}
function vecMag(values, baseIdx, nComp) {
    let mag2 = 0;
    for (let c = 0; c < nComp; c++)
        mag2 += values[baseIdx + c] ** 2;
    return Math.sqrt(mag2);
}
/**
 * Compute per-vertex scalar values from a VTP attribute, matching smgui exactly:
 *   - CellData scalar: average cell scalars to vertices
 *   - CellData multi-component: average raw vectors to vertices first, then compute magnitude
 *     (matches smgui _cell_to_vertex_interpolation_vector → norm; crease vertices cancel → low magnitude)
 *   - PointData scalar: use values directly
 *   - PointData multi-component: compute per-vertex magnitude directly
 * Returns null for unknown or missing attributes.
 */
function computeVertexValues(vtpFile, attribute) {
    if (!attribute)
        return null;
    if (attribute.startsWith('cell:')) {
        const arr = vtpFile.cellData.get(attribute.slice(5));
        if (!arr)
            return null;
        const nComp = arr.numberOfComponents;
        const vals = arr.values.toArray();
        if (nComp === 1) {
            return { values: cellToVertexAverage(vtpFile, vals), isMagnitude: false };
        }
        // Average vectors to vertices first, then take magnitude — crease vertices get lower values
        return { values: cellToVertexAverageMag(vtpFile, vals, nComp), isMagnitude: true };
    }
    if (attribute.startsWith('point:')) {
        const arr = vtpFile.pointData.get(attribute.slice(6));
        if (!arr)
            return null;
        const nComp = arr.numberOfComponents;
        const vals = arr.values.toArray();
        if (nComp === 1)
            return { values: vals, isMagnitude: false };
        // multi-component point data: per-vertex magnitude
        const nPts = vals.length / nComp;
        const mag = new Float64Array(nPts);
        for (let i = 0; i < nPts; i++)
            mag[i] = vecMag(vals, i * nComp, nComp);
        return { values: mag, isMagnitude: true };
    }
    return null;
}
/**
 * Build a color function from pre-computed per-vertex scalar values.
 * Auto domain matches smgui [min, max]. For magnitude attributes where all values are
 * nearly identical (e.g. PointData unit normals, range < 1% of max), fall back to [0, max].
 */
function makeColorFn(colorTheme, result) {
    if (colorTheme.name === 'uniform')
        return () => colorTheme.params.color;
    if (!result)
        return () => names_1.ColorNames.grey;
    const { values, isMagnitude } = result;
    const { colors, domain } = colorTheme.params;
    let lo, hi;
    if (domain.name === 'custom') {
        [lo, hi] = domain.params;
    }
    else {
        [lo, hi] = scalarRange(values);
        if (isMagnitude && hi > 0 && (hi - lo) / hi < 0.01)
            lo = 0; // degenerate unit-vector case
        if (domain.params.symmetric) {
            const absMax = Math.max(Math.abs(lo), Math.abs(hi));
            lo = -absMax;
            hi = absMax;
        }
    }
    if (hi - lo < 1e-9)
        hi = lo + 1e-9; // prevent degenerate domain
    const colorScale = scale_1.ColorScale.create({ listOrName: colors, domain: [lo, hi] });
    return (gid) => colorScale.color(values[gid]);
}
function createShape(vtpData, mesh, colorFn, props) {
    var _a;
    const scaleT = mat4_1.Mat4.fromUniformScaling((0, mat4_1.Mat4)(), props.scale);
    const baseTransforms = (_a = vtpData.transforms) !== null && _a !== void 0 ? _a : _identityTransforms;
    const transforms = baseTransforms.map(t => mat4_1.Mat4.mul((0, mat4_1.Mat4)(), t, scaleT));
    const { numberOfPoints } = vtpData.source;
    return shape_1.Shape.create('vtp-mesh', vtpData.source, mesh, colorFn, () => 1, (gid) => gid >= 0 ? `Vertex ${gid}` : '', transforms, numberOfPoints);
}
function makeShapeGetter() {
    let _vtpData;
    let _props;
    let _shape;
    let _mesh;
    let _colorFn;
    let _vertexResult = null;
    let _lastStatsAttr;
    let _lastStatsText = '';
    const getShape = async (ctx, vtpData, props, shape) => {
        const params = exports.VtpShapeParams;
        const needsNewMesh = !_vtpData || _vtpData.source !== vtpData.source;
        const newAttrKey = props.colorTheme.name === 'attribute' ? props.colorTheme.params.name : null;
        const oldAttrKey = _props
            ? (_props.colorTheme.name === 'attribute' ? _props.colorTheme.params.name : null)
            : undefined;
        const attributeChanged = needsNewMesh || newAttrKey !== oldAttrKey;
        const colorThemeChanged = !_props || !param_definition_1.ParamDefinition.isParamEqual(params.colorTheme, _props.colorTheme, props.colorTheme);
        const needsNewColor = needsNewMesh || attributeChanged || colorThemeChanged;
        const needsNewShape = needsNewMesh || needsNewColor ||
            (_vtpData === null || _vtpData === void 0 ? void 0 : _vtpData.transforms) !== vtpData.transforms ||
            !_props || _props.scale !== props.scale;
        if (needsNewMesh) {
            _mesh = await buildMesh(ctx, vtpData.source, shape === null || shape === void 0 ? void 0 : shape.geometry);
        }
        if (attributeChanged) {
            _vertexResult = newAttrKey ? computeVertexValues(vtpData.source, newAttrKey) : null;
            if (newAttrKey && newAttrKey !== _lastStatsAttr) {
                _lastStatsText = computeAttrStatsText(vtpData.source, newAttrKey);
                _lastStatsAttr = newAttrKey;
            }
            else if (!newAttrKey) {
                _lastStatsText = '';
                _lastStatsAttr = undefined;
            }
        }
        if (needsNewColor) {
            _colorFn = makeColorFn(props.colorTheme, _vertexResult);
        }
        if (needsNewShape) {
            _shape = createShape(vtpData, _mesh, _colorFn, props);
        }
        _vtpData = vtpData;
        if (needsNewMesh || needsNewColor || needsNewShape) {
            _props = (0, object_1.deepClone)(props);
        }
        return _shape;
    };
    const getStats = () => ({ text: _lastStatsText, attr: _lastStatsAttr });
    return { getShape, getStats };
}
function shapeFromVtp(source, params) {
    return mol_task_1.Task.create('Shape Provider', async () => {
        const getter = makeShapeGetter();
        const provider = {
            label: 'VTP Mesh',
            data: { source, transforms: params === null || params === void 0 ? void 0 : params.transforms },
            params: createVtpShapeParams(source, () => getter.getStats().text),
            getShape: getter.getShape,
            geometryUtils: mesh_1.Mesh.Utils,
        };
        return provider;
    });
}
