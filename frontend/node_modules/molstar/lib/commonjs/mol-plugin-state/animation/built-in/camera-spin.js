"use strict";
/**
 * Copyright (c) 2020-2026 mol* contributors, licensed under MIT, See LICENSE file for more info.
 *
 * @author David Sehnal <david.sehnal@gmail.com>
 * @author Alexander Rose <alexander.rose@weirdbyte.de>
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.AnimateCameraSpin = void 0;
const interpolate_1 = require("../../../mol-math/interpolate.js");
const quat_1 = require("../../../mol-math/linear-algebra/3d/quat.js");
const vec3_1 = require("../../../mol-math/linear-algebra/3d/vec3.js");
const param_definition_1 = require("../../../mol-util/param-definition.js");
const model_1 = require("../model.js");
const _dir = (0, vec3_1.Vec3)(), _axis = (0, vec3_1.Vec3)(), _rot = (0, quat_1.Quat)(), _up = (0, vec3_1.Vec3)(), _side = (0, vec3_1.Vec3)();
exports.AnimateCameraSpin = model_1.PluginStateAnimation.create({
    name: 'built-in.animate-camera-spin',
    display: { name: 'Camera Spin', description: 'Spin the 3D scene around the x-axis in view space' },
    isExportable: true,
    params: () => ({
        durationInMs: param_definition_1.ParamDefinition.Numeric(4000, { min: 100, max: 20000, step: 100 }),
        speed: param_definition_1.ParamDefinition.Numeric(1, { min: 1, max: 10, step: 1 }, { description: 'How many times to spin in the specified duration.' }),
        axis: param_definition_1.ParamDefinition.Vec3(vec3_1.Vec3.create(0, -1, 0), {}, { description: 'Axis of rotation in camera space' }),
    }),
    initialState: (_, ctx) => { var _a; return ({ snapshot: (_a = ctx.canvas3d) === null || _a === void 0 ? void 0 : _a.camera.getSnapshot() }); },
    getDuration: p => ({ kind: 'fixed', durationMs: p.durationInMs }),
    teardown: (_, state, ctx) => {
        var _a;
        (_a = ctx.canvas3d) === null || _a === void 0 ? void 0 : _a.requestCameraReset({ snapshot: state.snapshot, durationMs: 0 });
    },
    async apply(animState, t, ctx) {
        var _a, _b, _c;
        if (t.current === 0) {
            return { kind: 'next', state: animState };
        }
        const snapshot = animState.snapshot;
        if (snapshot.radiusMax < 0.0001) {
            return { kind: 'finished' };
        }
        const phase = t.animation
            ? ((_a = t.animation) === null || _a === void 0 ? void 0 : _a.currentFrame) / (t.animation.frameCount + 1)
            : (0, interpolate_1.clamp)(t.current / ctx.params.durationInMs, 0, 1);
        const angle = 2 * Math.PI * phase * ctx.params.speed;
        vec3_1.Vec3.sub(_dir, snapshot.position, snapshot.target);
        // Transform axis from camera space to world space
        vec3_1.Vec3.normalize(_axis, _dir); // Z = view direction
        vec3_1.Vec3.normalize(_up, snapshot.up); // Y = up
        vec3_1.Vec3.cross(_side, _up, _axis); // X = right
        vec3_1.Vec3.normalize(_side, _side);
        const a = (_b = ctx.params.axis) !== null && _b !== void 0 ? _b : vec3_1.Vec3.create(0, -1, 0); // default for backwards compatibility
        vec3_1.Vec3.set(_axis, a[0] * _side[0] + a[1] * _up[0] + a[2] * _axis[0], a[0] * _side[1] + a[1] * _up[1] + a[2] * _axis[1], a[0] * _side[2] + a[1] * _up[2] + a[2] * _axis[2]);
        vec3_1.Vec3.normalize(_axis, _axis);
        quat_1.Quat.setAxisAngle(_rot, _axis, angle);
        vec3_1.Vec3.transformQuat(_dir, _dir, _rot);
        vec3_1.Vec3.transformQuat(_up, snapshot.up, _rot);
        const position = vec3_1.Vec3.add((0, vec3_1.Vec3)(), snapshot.target, _dir);
        (_c = ctx.plugin.canvas3d) === null || _c === void 0 ? void 0 : _c.requestCameraReset({ snapshot: { ...snapshot, position, up: _up }, durationMs: 0 });
        if (phase >= 0.99999) {
            return { kind: 'finished' };
        }
        return { kind: 'next', state: animState };
    }
});
