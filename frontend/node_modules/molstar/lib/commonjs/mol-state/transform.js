"use strict";
/**
 * Copyright (c) 2018-2026 mol* contributors, licensed under MIT, See LICENSE file for more info.
 *
 * @author David Sehnal <david.sehnal@gmail.com>
 * @author Alexander Rose <alexander.rose@weirdbyte.de>
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.StateTransform = void 0;
const transformer_1 = require("./transformer.js");
const mol_util_1 = require("../mol-util/index.js");
const hash_functions_1 = require("../mol-data/util/hash-functions.js");
const param_definition_1 = require("../mol-util/param-definition.js");
const array_1 = require("../mol-util/array.js");
var Transform;
(function (Transform) {
    Transform.RootRef = '-=root=-';
    function isStateChange(a, b) {
        if (!b)
            return false;
        if (typeof b.isCollapsed !== 'undefined' && a.isCollapsed !== b.isCollapsed)
            return true;
        if (typeof b.isHidden !== 'undefined' && a.isHidden !== b.isHidden)
            return true;
        if (typeof b.isGhost !== 'undefined' && a.isGhost !== b.isGhost)
            return true;
        if (typeof b.isLocked !== 'undefined' && a.isLocked !== b.isLocked)
            return true;
        return false;
    }
    Transform.isStateChange = isStateChange;
    function assignState(a, b) {
        if (!b)
            return false;
        let changed = false;
        for (const k of Object.keys(b)) {
            const s = b[k], t = a[k];
            if (!!s === !!t)
                continue;
            changed = true;
            a[k] = s;
        }
        return changed;
    }
    Transform.assignState = assignState;
    function syncState(a, b) {
        if (!b)
            return false;
        let changed = false;
        for (const k of Object.keys(b)) {
            const s = b[k], t = a[k];
            if (!!s === !!t)
                continue;
            changed = true;
            if (s !== void 0) {
                a[k] = s;
            }
            else {
                delete a[k];
            }
        }
        for (const k of Object.keys(a)) {
            const s = b[k], t = a[k];
            if (!!s === !!t)
                continue;
            changed = true;
            if (s !== void 0) {
                a[k] = s;
            }
            else {
                delete a[k];
            }
        }
        return changed;
    }
    Transform.syncState = syncState;
    function create(parent, transformer, params, options) {
        const ref = options && options.ref ? options.ref : mol_util_1.UUID.create22();
        let tags = void 0;
        if (options && options.tags) {
            tags = typeof options.tags === 'string' ? [options.tags] : options.tags;
            if (tags.length === 0)
                tags = void 0;
            else
                tags.sort();
        }
        return {
            parent,
            transformer,
            state: (options === null || options === void 0 ? void 0 : options.state) || {},
            tags,
            ref,
            dependsOn: options && options.dependsOn,
            params,
            version: mol_util_1.UUID.create22()
        };
    }
    Transform.create = create;
    function withParams(t, params) {
        return { ...t, params, version: mol_util_1.UUID.create22() };
    }
    Transform.withParams = withParams;
    function withState(t, state) {
        if (!state)
            return t;
        return { ...t, state: { ...t.state, ...state } };
    }
    Transform.withState = withState;
    function withTags(t, newTags) {
        let tags = void 0;
        if (newTags) {
            tags = typeof newTags === 'string' ? [newTags] : newTags;
            if (tags.length === 0)
                tags = void 0;
            else
                tags.sort();
        }
        return { ...t, tags, version: mol_util_1.UUID.create22() };
    }
    Transform.withTags = withTags;
    function withDependsOn(t, newDependsOn) {
        let dependsOn = void 0;
        if (newDependsOn) {
            dependsOn = typeof newDependsOn === 'string' ? [newDependsOn] : newDependsOn;
            if (dependsOn.length === 0)
                dependsOn = void 0;
            else
                dependsOn.sort();
        }
        return { ...t, dependsOn, version: mol_util_1.UUID.create22() };
    }
    Transform.withDependsOn = withDependsOn;
    function withParent(t, parent) {
        return { ...t, parent, version: mol_util_1.UUID.create22() };
    }
    Transform.withParent = withParent;
    function createRoot(state) {
        return create(Transform.RootRef, transformer_1.StateTransformer.ROOT, {}, { ref: Transform.RootRef, state });
    }
    Transform.createRoot = createRoot;
    function hasTag(t, tag) {
        if (!t.tags)
            return false;
        return t.tags.indexOf(tag) >= 0;
    }
    Transform.hasTag = hasTag;
    function hasTags(t, tags) {
        if (!t.tags)
            return typeof tags !== 'string' && tags.length === 0;
        if (typeof tags === 'string')
            return hasTag(t, tags);
        for (const tag of tags) {
            if (t.tags.indexOf(tag) < 0)
                return false;
        }
        return true;
    }
    Transform.hasTags = hasTags;
    /**
     * Returns the default param schema for the transform's transformer, caching
     * the result on the transform instance. Returns `undefined` if the transformer
     * has no `params` definition or if calling it throws.
     */
    function tryGetDefaultParamDefinition(t) {
        const any = t;
        if (any._defaultSchemaSet)
            return any._defaultSchema;
        any._defaultSchemaSet = true;
        try {
            const def = t.transformer.definition;
            if (def.params) {
                any._defaultSchema = def.params(undefined, undefined);
            }
        }
        catch (_a) {
            // Schema could not be obtained.
        }
        return any._defaultSchema;
    }
    Transform.tryGetDefaultParamDefinition = tryGetDefaultParamDefinition;
    /**
     * Compute the effective set of sibling-like dependencies for a transform.
     *
     * Combines (in order, de-duplicated):
     *   1. Explicit `t.dependsOn` (back-compat / non-param refs).
     *   2. Refs from `transformer.definition.getDependencies(params)` if defined.
     *   3. Refs collected from `PD.ValueRef` / `PD.DataRef` parameter values.
     *
     * Self-references and the root ref are filtered out. If the param schema
     * can't be obtained, auto-derivation falls back to a structural scan of
     * parameter values for `{ ref, getValue }` shaped objects.
     */
    function getEffectiveDependsOn(t) {
        const out = [];
        if (t.dependsOn) {
            for (const r of t.dependsOn) {
                (0, array_1.arraySetAdd)(out, r);
            }
        }
        const def = t.transformer.definition;
        const params = t.params;
        if (def.getDependencies && params) {
            try {
                const extra = def.getDependencies(params);
                if (extra) {
                    for (const r of extra) {
                        (0, array_1.arraySetAdd)(out, r);
                    }
                }
            }
            catch (_a) {
                // Keep reconciliation robust if a user hook misbehaves.
            }
        }
        if (params) {
            const schema = tryGetDefaultParamDefinition(t);
            if (schema) {
                param_definition_1.ParamDefinition.collectRefs(schema, params, out);
            }
            else {
                collectStructuralRefs(params, out);
            }
        }
        // Filter out self-references and the root ref.
        (0, array_1.arraySetRemove)(out, t.ref);
        (0, array_1.arraySetRemove)(out, Transform.RootRef);
        return out;
    }
    Transform.getEffectiveDependsOn = getEffectiveDependsOn;
    function collectStructuralRefs(value, out, depth = 0) {
        if (!value || typeof value !== 'object' || depth > 6)
            return out;
        if (Array.isArray(value)) {
            for (const v of value) {
                collectStructuralRefs(v, out, depth + 1);
            }
            return out;
        }
        const ref = value.ref;
        if (typeof ref === 'string' && typeof value.getValue === 'function') {
            (0, array_1.arraySetAdd)(out, ref);
            return out;
        }
        for (const k of Object.keys(value)) {
            collectStructuralRefs(value[k], out, depth + 1);
        }
        return out;
    }
    const _emptyParams = {};
    /** Updates the version of the transform to be computed as hash of the parameters */
    function setParamsHashVersion(t) {
        var _a, _b;
        let version;
        try {
            version = (0, hash_functions_1.hashMurmur128o)((_a = t.params) !== null && _a !== void 0 ? _a : _emptyParams);
        }
        catch (_c) {
            const pToJson = t.transformer.definition.customSerialization
                ? t.transformer.definition.customSerialization.toJSON
                : _id;
            version = (0, hash_functions_1.hashMurmur128o)(pToJson((_b = t.params) !== null && _b !== void 0 ? _b : _emptyParams));
        }
        t.version = version;
    }
    Transform.setParamsHashVersion = setParamsHashVersion;
    function _id(x) { return x; }
    function toJSON(t) {
        const pToJson = t.transformer.definition.customSerialization
            ? t.transformer.definition.customSerialization.toJSON
            : _id;
        let state = void 0;
        for (const k of Object.keys(t.state)) {
            const s = t.state[k];
            if (!s)
                continue;
            if (!state)
                state = {};
            state[k] = true;
        }
        return {
            parent: t.parent,
            transformer: t.transformer.id,
            params: t.params ? pToJson(t.params) : void 0,
            state,
            tags: t.tags,
            ref: t.ref,
            dependsOn: t.dependsOn,
            version: t.version
        };
    }
    Transform.toJSON = toJSON;
    function fromJSON(t) {
        const transformer = transformer_1.StateTransformer.get(t.transformer);
        const pFromJson = transformer.definition.customSerialization
            ? transformer.definition.customSerialization.fromJSON
            : _id;
        return {
            parent: t.parent,
            transformer,
            params: t.params ? pFromJson(t.params) : void 0,
            state: t.state || {},
            tags: t.tags,
            ref: t.ref,
            dependsOn: t.dependsOn,
            version: t.version
        };
    }
    Transform.fromJSON = fromJSON;
})(Transform || (exports.StateTransform = Transform = {}));
