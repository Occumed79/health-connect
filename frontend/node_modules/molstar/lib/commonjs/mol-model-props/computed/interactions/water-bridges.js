"use strict";
/**
 * Copyright (c) 2026 mol* contributors, licensed under MIT, See LICENSE file for more info.
 *
 * @author Sebastian Bittrich <sebastian.m.bittrich@gmail.com>
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.WaterBridgesProvider = exports.WaterBridgesParams = void 0;
exports.findWaterBridgeContacts = findWaterBridgeContacts;
const structure_1 = require("../../../mol-model/structure.js");
const linear_algebra_1 = require("../../../mol-math/linear-algebra.js");
const param_definition_1 = require("../../../mol-util/param-definition.js");
const types_1 = require("../../../mol-model/structure/model/types.js");
const lookup3d_1 = require("../../../mol-model/structure/structure/util/lookup3d.js");
const features_1 = require("./features.js");
const common_1 = require("./common.js");
const hydrogen_bonds_1 = require("./hydrogen-bonds.js");
const misc_1 = require("../../../mol-math/misc.js");
const hash_functions_1 = require("../../../mol-data/util/hash-functions.js");
exports.WaterBridgesParams = {
    backbone: param_definition_1.ParamDefinition.Boolean(true, { description: 'Include backbone hydrogen bonds' }),
    ignoreHydrogens: param_definition_1.ParamDefinition.Boolean(true, { description: 'Ignore explicit hydrogens in geometric constraints' }),
    legDistMin: param_definition_1.ParamDefinition.Numeric(2.5, { min: 1, max: 4, step: 0.1 }, { description: 'Minimum leg distance (Å)' }),
    legDistMax: param_definition_1.ParamDefinition.Numeric(4.1, { min: 1, max: 6, step: 0.1 }, { description: 'Maximum leg distance (Å)' }),
    donAngleDevMax: param_definition_1.ParamDefinition.Numeric(80, { min: 0, max: 180, step: 1 }, { description: 'Max deviation from ideal donor angle' }),
    accAngleDevMax: param_definition_1.ParamDefinition.Numeric(50, { min: 0, max: 180, step: 1 }, { description: 'Max deviation from ideal acceptor angle' }),
    donOutOfPlaneAngleMax: param_definition_1.ParamDefinition.Numeric(45, { min: 0, max: 180, step: 1 }),
    accOutOfPlaneAngleMax: param_definition_1.ParamDefinition.Numeric(90, { min: 0, max: 180, step: 1 }),
    omegaMin: param_definition_1.ParamDefinition.Numeric(71, { min: 0, max: 180, step: 1 }, { description: 'Minimum A–W–B angle (°)' }),
    omegaMax: param_definition_1.ParamDefinition.Numeric(140, { min: 0, max: 180, step: 1 }, { description: 'Maximum A–W–B angle (°)' }),
};
exports.WaterBridgesProvider = {
    requiredFeatures: new Set([common_1.FeatureType.HydrogenDonor, common_1.FeatureType.HydrogenAcceptor]),
    params: exports.WaterBridgesParams,
    find: findWaterBridgeContacts,
};
function isWater(unit, index) {
    return unit.model.atomicHierarchy.derived.residue.moleculeType[unit.residueIndex[unit.elements[index]]] === types_1.MoleculeType.Water;
}
function isBackboneAtom(unit, index) {
    const element = unit.elements[index];
    const moleculeType = unit.model.atomicHierarchy.derived.residue.moleculeType[unit.residueIndex[element]];
    if (moleculeType !== types_1.MoleculeType.Protein && moleculeType !== types_1.MoleculeType.RNA && moleculeType !== types_1.MoleculeType.DNA) {
        return false;
    }
    const atomId = unit.model.atomicHierarchy.atoms.label_atom_id.value(element);
    if (moleculeType === types_1.MoleculeType.Protein) {
        return types_1.ProteinBackboneAtoms.has(atomId);
    }
    return types_1.NucleicBackboneAtoms.has(atomId);
}
const _lookupCtx = (0, lookup3d_1.StructureLookup3DResultContext)();
function featureKey(unitId, featureIndex) {
    return (0, hash_functions_1.cantorPairing)(unitId, featureIndex);
}
function getBestBridge(best, donorKey, acceptorKey) {
    var _a;
    return (_a = best.get(donorKey)) === null || _a === void 0 ? void 0 : _a.get(acceptorKey);
}
function setBestBridge(best, donorKey, acceptorKey, value) {
    let acceptors = best.get(donorKey);
    if (acceptors === undefined) {
        acceptors = new Map();
        best.set(donorKey, acceptors);
    }
    acceptors.set(acceptorKey, value);
}
function bestBridgeValues(best) {
    const values = [];
    for (const acceptors of best.values()) {
        for (const value of acceptors.values())
            values.push(value);
    }
    return values;
}
function checkOmega(don, posW, acc, cosOmegaMin, cosOmegaMax) {
    const ax = don.x - posW[0];
    const ay = don.y - posW[1];
    const az = don.z - posW[2];
    const bx = acc.x - posW[0];
    const by = acc.y - posW[1];
    const bz = acc.z - posW[2];
    const aLenSq = ax * ax + ay * ay + az * az;
    const bLenSq = bx * bx + by * by + bz * bz;
    if (aLenSq === 0 || bLenSq === 0)
        return false;
    const cosOmega = (ax * bx + ay * by + az * bz) / Math.sqrt(aLenSq * bLenSq);
    // cos decreases monotonically on [0, pi], so:
    // omega >= omegaMin && omega <= omegaMax
    // is equivalent to:
    // cos(omega) <= cos(omegaMin) && cos(omega) >= cos(omegaMax)
    return cosOmega <= cosOmegaMin && cosOmega >= cosOmegaMax;
}
function findWaterBridgeContacts(structure, unitsFeatures, props) {
    const legOpts = {
        ignoreHydrogens: props.ignoreHydrogens,
        includeBackbone: props.backbone,
        maxAccAngleDev: (0, misc_1.degToRad)(props.accAngleDevMax),
        maxDonAngleDev: (0, misc_1.degToRad)(props.donAngleDevMax),
        maxAccOutOfPlaneAngle: (0, misc_1.degToRad)(props.accOutOfPlaneAngleMax),
        maxDonOutOfPlaneAngle: (0, misc_1.degToRad)(props.donOutOfPlaneAngleMax),
    };
    const legDistMinSq = props.legDistMin * props.legDistMin;
    const legDistMaxSq = props.legDistMax * props.legDistMax;
    const omegaMinRad = (0, misc_1.degToRad)(props.omegaMin);
    const omegaMaxRad = (0, misc_1.degToRad)(props.omegaMax);
    if (omegaMinRad > omegaMaxRad)
        return [];
    const cosOmegaMin = Math.cos(omegaMinRad);
    const cosOmegaMax = Math.cos(omegaMaxRad);
    // Best bridge per unique donor/acceptor feature pair across all water molecules.
    const best = new Map();
    const wPos = (0, linear_algebra_1.Vec3)();
    const candidatePos = (0, linear_algebra_1.Vec3)();
    for (const unitW of structure.units) {
        if (!structure_1.Unit.isAtomic(unitW))
            continue;
        const featW = unitsFeatures.get(unitW.id);
        if (!featW || featW.count === 0)
            continue;
        // Map each water-oxygen local index to its acceptor and donor feature indices.
        const waterMap = new Map();
        for (let fi = 0; fi < featW.count; fi++) {
            const mi = featW.members[featW.offsets[fi]];
            if (!isWater(unitW, mi))
                continue;
            const t = featW.types[fi];
            if (t !== common_1.FeatureType.HydrogenAcceptor && t !== common_1.FeatureType.HydrogenDonor)
                continue;
            let e = waterMap.get(mi);
            if (!e)
                waterMap.set(mi, (e = { acc: undefined, don: undefined }));
            if (t === common_1.FeatureType.HydrogenAcceptor)
                e.acc = fi;
            else
                e.don = fi;
        }
        if (waterMap.size === 0)
            continue;
        const infoWAcc = features_1.Features.Info(structure, unitW, featW);
        const infoWDon = features_1.Features.Info(structure, unitW, featW);
        for (const [waterAtomIdx, { acc: accFW, don: donFW }] of waterMap) {
            if (accFW === undefined || donFW === undefined)
                continue;
            unitW.conformation.position(unitW.elements[waterAtomIdx], wPos);
            infoWAcc.feature = accFW;
            infoWDon.feature = donFW;
            const { count, indices, units: hitUnits } = structure.lookup3d.find(wPos[0], wPos[1], wPos[2], props.legDistMax, _lookupCtx);
            const donors = [];
            const acceptors = [];
            const donorKeys = new Set();
            const acceptorKeys = new Set();
            for (let r = 0; r < count; r++) {
                const hitUnit = hitUnits[r];
                if (!structure_1.Unit.isAtomic(hitUnit))
                    continue;
                const atomicUnit = hitUnit;
                const hitLocalIdx = indices[r];
                // Only skip the water atom itself. Other atoms in the same unit can still be valid.
                if (atomicUnit === unitW && hitLocalIdx === waterAtomIdx)
                    continue;
                if (isWater(atomicUnit, hitLocalIdx))
                    continue;
                const hitFeat = unitsFeatures.get(atomicUnit.id);
                if (!hitFeat || hitFeat.count === 0)
                    continue;
                const infoHit = features_1.Features.Info(structure, atomicUnit, hitFeat);
                const { indices: fIdxs, offsets: fOff } = hitFeat.elementsIndex;
                for (let k = fOff[hitLocalIdx], kl = fOff[hitLocalIdx + 1]; k < kl; k++) {
                    const fi = fIdxs[k];
                    const fType = hitFeat.types[fi];
                    if (fType !== common_1.FeatureType.HydrogenDonor && fType !== common_1.FeatureType.HydrogenAcceptor)
                        continue;
                    const memberIdx = hitFeat.members[hitFeat.offsets[fi]];
                    if (!props.backbone && isBackboneAtom(atomicUnit, memberIdx))
                        continue;
                    atomicUnit.conformation.position(atomicUnit.elements[memberIdx], candidatePos);
                    const distSq = linear_algebra_1.Vec3.squaredDistance(candidatePos, wPos);
                    if (distSq < legDistMinSq || distSq > legDistMaxSq)
                        continue;
                    infoHit.feature = fi;
                    if (fType === common_1.FeatureType.HydrogenDonor) {
                        const key = featureKey(atomicUnit.id, fi);
                        if (donorKeys.has(key))
                            continue;
                        if ((0, hydrogen_bonds_1.checkGeometry)(structure, infoHit, infoWAcc, legOpts)) {
                            donorKeys.add(key);
                            donors.push({
                                unit: atomicUnit,
                                featureIdx: fi,
                                memberIdx,
                                x: candidatePos[0],
                                y: candidatePos[1],
                                z: candidatePos[2],
                                distSq,
                            });
                        }
                    }
                    else {
                        const key = featureKey(atomicUnit.id, fi);
                        if (acceptorKeys.has(key))
                            continue;
                        if ((0, hydrogen_bonds_1.checkGeometry)(structure, infoWDon, infoHit, legOpts)) {
                            acceptorKeys.add(key);
                            acceptors.push({
                                unit: atomicUnit,
                                featureIdx: fi,
                                memberIdx,
                                x: candidatePos[0],
                                y: candidatePos[1],
                                z: candidatePos[2],
                                distSq,
                            });
                        }
                    }
                }
            }
            for (const don of donors) {
                for (const acc of acceptors) {
                    // Reject bridges where donor and acceptor are the same physical atom
                    // represented by different feature indices.
                    if (don.unit === acc.unit && don.memberIdx === acc.memberIdx)
                        continue;
                    if (!checkOmega(don, wPos, acc, cosOmegaMin, cosOmegaMax))
                        continue;
                    const combinedDistSq = don.distSq + acc.distSq;
                    const donorKey = featureKey(don.unit.id, don.featureIdx);
                    const acceptorKey = featureKey(acc.unit.id, acc.featureIdx);
                    const existing = getBestBridge(best, donorKey, acceptorKey);
                    if (!existing || combinedDistSq < existing.combinedDistSq) {
                        setBestBridge(best, donorKey, acceptorKey, {
                            contact: {
                                unitA: don.unit.id,
                                indexA: don.featureIdx,
                                unitB: acc.unit.id,
                                indexB: acc.featureIdx,
                                unitM: unitW.id,
                                indexMA: accFW,
                                indexMB: donFW,
                                props: { type: common_1.InteractionType.WaterBridge, flag: common_1.InteractionFlag.None },
                            },
                            combinedDistSq,
                        });
                    }
                }
            }
        }
    }
    return bestBridgeValues(best).map(e => e.contact);
}
