/**
 * Copyright (c) 2018-2026 Mol* contributors, licensed under MIT, See LICENSE file for more info.
 *
 * @author David Sehnal <david.sehnal@gmail.com>
 */
import { Camera } from '../camera.js';
import { EasingFunction } from '../../mol-math/easing.js';
export { CameraTransitionManager };
export interface CameraTransitionOptions {
    /** If present, approximates the transion between, [current] -> [keyframes] -> -> [target] */
    keyframes?: CameraTransitionManager.TransitionKeyframes;
    /** Global easing, if easing is specified for keyframes, the "end" frame value is used  */
    easing?: EasingFunction;
}
declare class CameraTransitionManager {
    private camera;
    private t;
    private func;
    private start;
    inTransition: boolean;
    private durationMs;
    private _source;
    private _target;
    private _options;
    private _current;
    get source(): Readonly<Camera.Snapshot>;
    get target(): Readonly<Camera.Snapshot>;
    apply(to: Partial<Camera.Snapshot>, durationMs?: number, transition?: CameraTransitionManager.TransitionFunc, options?: CameraTransitionOptions): void;
    tick(t: number): void;
    private finish;
    private update;
    constructor(camera: Camera);
}
declare namespace CameraTransitionManager {
    type TransitionKeyframes = {
        t: number;
        snapshot: Partial<Camera.Snapshot>;
        easing?: EasingFunction;
    }[];
    type TransitionFunc = (out: Camera.Snapshot, t: number, source: Camera.Snapshot, target: Camera.Snapshot, options?: {
        keyframes?: TransitionKeyframes;
    }) => void;
    function defaultTransition(out: Camera.Snapshot, t_: number, source_: Camera.Snapshot, target_: Camera.Snapshot, options?: CameraTransitionOptions): void;
}
