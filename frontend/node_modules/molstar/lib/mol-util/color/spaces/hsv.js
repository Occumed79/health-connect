/**
 * Copyright (c) 2026 mol* contributors, licensed under MIT, See LICENSE file for more info.
 *
 * @author ReliaSolve <russ@reliasolve.com>
 *
 * Adapted from kin-parser.ts file from the NGL project:
 * @author Alexander Rose <alexander.rose@weirdbyte.de>
 * Adapted from hsl.ts in this same directory:
 * @author David Sehnal <david.sehnal@gmail.com>
 */
import { Rgb } from './rgb.js';
export { Hsv };
function Hsv() {
    return Hsv.zero();
}
(function (Hsv) {
    function zero() {
        const out = [0.0, 0.0, 0.0];
        out[0] = 0;
        return out;
    }
    Hsv.zero = zero;
    /** Copy values from an array-like 3-tuple into `out`. */
    function fromArray(arr) {
        var _a, _b, _c;
        const out = Hsv.zero();
        out[0] = (_a = arr[0]) !== null && _a !== void 0 ? _a : 0;
        out[1] = (_b = arr[1]) !== null && _b !== void 0 ? _b : 0;
        out[2] = (_c = arr[2]) !== null && _c !== void 0 ? _c : 0;
        return out;
    }
    Hsv.fromArray = fromArray;
    const _rgb = Rgb();
    function toColor(hsv) {
        toRgb(_rgb, hsv);
        return Rgb.toColor(_rgb);
    }
    Hsv.toColor = toColor;
    function toRgb(out, hsv) {
        let [h, s, v] = hsv;
        h /= 360;
        s /= 100;
        v /= 100;
        let r = 0, g = 0, b = 0;
        const i = Math.floor(h * 6);
        const f = h * 6 - i;
        const p = v * (1 - s);
        const q = v * (1 - f * s);
        const t = v * (1 - (1 - f) * s);
        switch (i % 6) {
            case 0:
                r = v;
                g = t;
                b = p;
                break;
            case 1:
                r = q;
                g = v;
                b = p;
                break;
            case 2:
                r = p;
                g = v;
                b = t;
                break;
            case 3:
                r = p;
                g = q;
                b = v;
                break;
            case 4:
                r = t;
                g = p;
                b = v;
                break;
            case 5:
                r = v;
                g = p;
                b = q;
                break;
        }
        out[0] = r;
        out[1] = g;
        out[2] = b;
        return out;
    }
    Hsv.toRgb = toRgb;
})(Hsv || (Hsv = {}));
